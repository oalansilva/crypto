[pytest]
asyncio_mode = auto
addopts = -p no:anyio -k "not e2e"
markers =
    e2e: frontend end-to-end tests (disabled by default)
    postgres: test requires isolated PostgreSQL state (schemas and tables are reset before the test)
timeout = 120
timeout_method = thread
timeout_func_only = true
