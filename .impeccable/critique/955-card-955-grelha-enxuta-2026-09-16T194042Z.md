# Snapshot — Design critic (sem-tela, 1 crítico, NÃO A/B) · card #955 `card-955-grelha-enxuta` · pós-rework 1/1

- Card: #955 — kaizen: grelha enxuta em Em Refinamento — 3 seções, 1 rodada, delta do body; vocabulário, riscos e superset ficam no Design
- Change: `card-955-grelha-enxuta`
- Critic: 1 isolado (NOT A/B); sem transcript do pai; sem nested agent; sem Assessment A/B
- Modelo: Grok 4.6 (`cursor-grok-4.6-high`)
- proxy modelo: design-critic → Grok 4.6 (cursor-grok-4.6-high)
- UTC: 2026-09-16T19:40:42Z
- Round: crítico pós-rework 1/1 (teto D4 sem-tela = 1 autor + 1 crítico + 1 rework; segundo rework só com P0 novo de produto justificado no prompt)
- Snapshot anterior (crítico 1/1, pré-rework): `.impeccable/critique/955-card-955-grelha-enxuta-2026-09-16T192211Z.md`
- Tuple (este isolado): Write produto deny. Esta onda só `.impeccable/critique/**`. Não T5. Não `process_event`. Não commit/push. Não `move_agent_to_root`. Não editar `design.md` / proposal / tasks / specs / HTML / `backend/` / `frontend/src/`.
- Board / issue: REST `GET /repos/oalansilva/crypto/issues/955` (não `gh issue view`). OPEN; labels `priority:P1` `type:operacao` `kaizen`. Prompt do pai: Status Project 1 = Design. UI impact: none (sem ecrã). Prototype N/A.
- Digest `design.md` **medido**: sha256 `069a881aea1fc8116e34705c77a758e3adfcc27a159f0b1b10a14349694d48ca` · **2621** palavras (`wc -w`) · 17388 bytes · 161 linhas. Ficheiro `.design-digest` ainda `4fa18fd5…` (pré-rework) — P3 higiene, não contrato visível.
- `## Design Critique` / `Design Agent verdict` em `design.md`: **presentes e stale** (cola do crítico 1/1: Q4=A honrada; `proposal.md` sem recopiar). Pai substitui após este snapshot. Este crítico **não** edita `design.md`.
- UI impact: **none** (processo / harness `grill-card` + runbook + kaizen; nenhuma rota, shell, componente, token ou copy de ecrã do CriptoFarol)
- Prototype: **N/A** — `UI impact: none`; zero HTML `frontend/public/prototypes/*955*`; proto dir sem artefacto 955. Justificativa no `design.md` ## Prototype não vazia. Impeccable visual / Playwright / `DESIGN.md` = N/A. Este ficheiro é o snapshot git-tracked da crítica de processo (T7).
- Gate tokens parseáveis (linhas próprias no `design.md`):
  - `UI impact: none`
  - `live_route: N/A harness-only; no product route`
  - `surface: new`
- Clone gate isento via `UI impact: none` + `live_route: N/A` justificado (frase seguinte: mudança de processo/harness, nenhuma tela CriptoFarol, sem rota de catálogo emprestada) + `surface: new` (padrão aceite em #821/#854/#859/#893/#927 — **não** é tela Cripto nova). **Nenhuma** rota de catálogo emprestada (`/monitor` `/favorites` `/combo/*` `landing`).
- Bloco D4 teto: **presente** no `design.md` (texto exacto da skill; match byte-a-byte).
- Method: issue #955 REST (Problema / História / Entra / Não entra / Q1=A Q2=A Q3=B; Q4=A no body grelhado, **não reescrito** — Non-Goal); proposal/design/tasks/specs **depois do rework**; diff issue↔proposal nas 4 seções; live adapter ainda DoD 6 (pré-Apply, pin Cripto `v1.1.15`). Q1–Q3 **não reabertas**.

---

## Surfaces lidas

| Superfície | Classificação |
| --- | --- |
| Issue #955 body (REST) | lido — Problema / História / Entra / Não entra / Vocabulário / critérios / Riscos; Q1=A Q2=A Q3=B Q4=A (body pré-T6, não reescrito) |
| `openspec/changes/card-955-grelha-enxuta/{proposal,design,tasks}.md` | lido **pós-rework** |
| `openspec/changes/card-955-grelha-enxuta/specs/{grill-card,covenant-flow,cursor-harness,kaizen-continuous-improvement}/spec.md` | lido **pós-rework** |
| Live `.cursor/skills/grill-card/SKILL.md` | lido — DoD vigente 6 seções; disparo «body não tem as 6»; tecto #809; vendor `grilling` intacto (pré-Apply) |
| Snapshot crítico 1/1 `955-…-2026-09-16T192211Z.md` | lido — P2 «Gist deixa de ser *superset*» era o furo T7; este rework fecha-o |
| `frontend/src/**`, `backend/` de app, proto HTML 955 | **none** / ausente |
| Catálogo `/monitor` `/favorites` `/combo/*` `landing` | **não emprestado** |

Nenhuma superfície de produto nova/alterada ficou sem classificação. Prototype N/A justificado.

Este crítico **não** editou `design.md` / proposal / tasks / specs / produto.

---

## Brief (só neste snapshot)

Quem sofre é o operador do board: Em Refinamento custa mais do que Design porque a grelha reescreve o body inteiro, produz Vocabulário/Riscos que o Design volta a produzir, e dispara em card já nítido. Q1–Q3 fechadas no cartão (A / A / B) — **não reabertas**. Alan T6 substituiu Q4=A: T7 não validava Why de uma linha; Gist volta a ser *superset*.

Direction do rework: `proposal.md` copia Problema / História / Entra / Não entra do body (contrato do issue, não uma linha Why); Q1=A Q2=A Q3=B intactos no texto copiado; bullet Q4 no Entra/Não entra = substituição autorizada (copia, não reentrevista; *como* no `design.md`). Golden: `proposal.md` **sem** `## Problema` / `## História` / `## Entra` falha. DoD da grelha depois do pin = 3 seções; Vocabulário e Riscos no `design.md`; skip nítido `card nítido; sem grill`; `G_design` não afrouxa; kaizen ≤1,5 sessões/card na janela de 10; entrega produto `oalansilva/covenant-flow` patch (esperado `v1.1.16`) + `implantar --pin`. Vigente 6 até o pin. Vendor `grilling`, T1, colunas, teto Design 1+1+1, clone da página viva, `backend/` / `frontend/src/` intocados.

Q1=A Q2=A Q3=B não reabertas. Não entra (excepto o bullet Q4 autorizado) permanece Non-Goal.

---

## Critique (rubrica UI none + rubrica extra do rework)

### 1. Gate parseável

Três tokens em linha própria no `design.md`. Sem-tela declara ausência (`none` / `N/A`) + justificativa na frase seguinte. `live_route` não é rota de catálogo. `surface: new` = isenção clone-gate, não ecrã novo. `## Prototype` e `## Prototype Validation` N/A com texto não vazio. Sem HTML / proto / `DESIGN.md`. Bloco D4 exacto.

Tokens **ok**. D4 **ok**. Sem proto **ok**. Nenhuma rodada extra para o parser. Sem segundo rework por parser.

### 2. Escopo vs issue (colunas, T1, vendor, 1+1+1, clone viva, produto+pin, zero app)

Mapeado:

| Entra do cartão | Pacote pós-rework |
| --- | --- |
| Colunas Em Refinamento vs Design ficam | Non-Goals; spec covenant-flow MUST NOT FSM/`process-fsm.yaml`; task 2.4 |
| DoD alvo 3 seções; Vocabulário/Riscos no Design | D1; spec grill-card ADDED empty-frontier 3 + REMOVED 6; `## Vocabulário` / `## Risks` neste `design.md`; tasks 1.1, 2.3 |
| Q1=A uma passagem teto 5 | D2; spec one-pass; task 1.2 — **intacta** |
| Q2=A skip nítido; pedido explícito continua | D3; spec parent-skip; tasks 2.1, 5.2 — **intacta** |
| Q3=B sem segunda passagem | D4; spec second-pass golden fail; task 1.2 — **intacta** |
| Q4 **substituída** (T6): Gist = *superset*; proposal copia história; *como* no design.md | D6; proposal **com** `## Problema`/`## História`/`## Entra`/`## Não entra`; spec cursor-harness + covenant-flow + grill-card golden fail se headings ausentes; tasks 2.2–2.3, 5.1 |
| `G_design` continua pacote + comentário | D6; spec cursor-harness + covenant-flow; task 2.2 |
| Medida kaizen 1,5 / 10 (*como*) | D10; spec kaizen; task 3.1 |
| Produto + pin; UI none; sem BREAKING CriptoFarol | D11 + Apply contract; spec pin patch; tasks 6–8 |
| T1 / vendor / 1+1+1 / clone viva / #667 #755 #809 / bodies já grelhados / app code | Non-Goals + tasks 1.4, 2.4, 7.2 |

Não entra **não reaberto** (salvo o bullet Q4, delta autorizado T6). Entrega é produto `covenant-flow` + pin, não dual-write de lei em `.dsh/` / `.grok/` / `.opencode/`. Zero `backend/` / `frontend/src/`.

### 3. Q1–Q3 intactos; Q4 substituída (não reabrir Q1–Q3)

- **Q1=A:** adapter pára após uma rodada ≤5 Qs de produto; vendor `grilling` intocado; árvore Matt MAY no Design, MUST NOT em Em Refinamento. Golden dump 6 Qs ou segunda passagem → fail. Texto do issue em `proposal.md` Entra **byte-idêntico**.
- **Q2=A:** skip nítido no pai; comentário exacto `card nítido; sem grill` (idempotente, ≠ T1). Detector pytest = `## Problema` não-vazio **e** (`## Entra` ou `## Não entra`) não-vazio. Pedido explícito continua. Texto do issue em `proposal.md` Entra **byte-idêntico**.
- **Q3=B:** furo da única passagem segue listado (Entra e/ou dump); MUST NOT `## Riscos` no issue; pode voltar como P0 de produto na crítica — aceite do cartão. Texto do issue em `proposal.md` Entra **byte-idêntico**.
- **Q4 substituída (T6, não reaberta como Q):** `proposal.md` copia Problema/História/Entra/Não entra + Q1–Q3; *como* no `design.md`; grelha não reentrevista. Golden: proposal sem esses headings falha. Alternativa rejeitada = Q4=A (*espelho do como*; história só no GitHub). Body #955 mantém Q4=A — Non-Goal «não reescrever bodies já grelhados»; D6 é o contrato daqui para a frente.

### 4. Rubrica extra deste rework — `proposal.md` = contrato do issue (não Why de uma linha)

Diff REST issue ↔ `proposal.md` (Python, seções `## Problema` / `## História` / `## Entra` / `## Não entra`):

| Seção | Resultado |
| --- | --- |
| Problema | **byte-idêntico** ao issue |
| História | **byte-idêntico** ao issue |
| Entra | idêntico **excepto** o bullet Q4 (issue `Q4=A` história só no cartão → proposal `Q4 substituída` Gist *superset* + copia). Q1=A Q2=A Q3=B e os outros bullets **byte-idênticos** |
| Não entra | idêntico **excepto** o bullet Q4 (issue «não redigir quem sofre» → proposal «não reentrevistar / não inventar; copia do body»). Restantes bullets **byte-idênticos** |

`## Why` permanece como ponte OpenSpec (origem + substituição), **não** substitui as quatro seções. T7 valida o contrato nas headings do issue, não numa linha Why. Specs + task 5.1 cravam o golden: fixture sem `## Problema` / `## História` / `## Entra` falha; fixture que copia do issue passa.

### 5. DoD 3 / Vocabulário-Riscos / G_design / D4 / proto

- Headings após grelha que corre: Problema, História, Entra (`Não entra` MAY). Critérios dentro de Entra. Fronteira vazia = 3 + zero decisão de operador. Comentário T1 **texto pinado** (só o *quando*).
- Vocabulário e Riscos neste `design.md`, não no body alvo pós-pin. Migration: #955 e bodies já grelhados **mantêm 6** até/apesar do pin; DoD 3 vale daqui para a frente.
- `G_design` T5 continua a exigir `proposal.md` + `design.md` + `tasks.md` + `specs/**` + clone-gate + comentário. Apply lê Gist + `openspec/changes/`, não o body como spec paralela. Briefing OpenSpec = 3 seções; furo → comentar e permanecer em Design. «Card primeiro» = Gist *superset* (história copiada + *como*) — MUST NOT «só o como».
- Bloco D4 colado exacto.
- Sem proto / sem HTML.

### 6. Regressão operacional (não bloqueante)

- Live `grill-card` ainda exige 6 seções e dispara na ausência das 6. Esperado até o pin. Rollback = pin `v1.1.15`.
- Tecto #809 (frase exacta Other/silêncio) e relay `todas as options` / `não colapsa` permanecem. N≥2 / D5 / ramos Cursor-Grok vs dsh intactos. Spec MODIFIED ceiling não reabre #809.
- Skip nítido **não** é T1. Alan continua único em T1.
- Peles thin MUST Read ≤8; inchar stubs = regressão do needle.
- `## Design Critique` no `design.md` ainda descreve o pacote **pré-rework**. Pai cola este snapshot; crítico não edita produto.

### 7. Apply contract executável

Paths fechados: produto `.cursor/skills/{grill-card,covenant-flow,openspec-new-change,openspec-ff-change,kaizen}/SKILL.md`; goldens `scripts/process-fsm/test_grill_card.py` + fixtures (incl. proposal sem headings → fail); tag patch (esperado `v1.1.16`, Apply confirma origin); `implantar --pin` no Cripto; overlay `pin:`. MUST NOT vendor `grilling`, `process-fsm.yaml`, `AGENTS.md` always-on, `backend/**`, `frontend/src/**`, dual-write lei, HTML, reabrir #667/#755/#809, reescrever bodies já grelhados.

Pré-Apply (correcto nesta coluna): DoD 6 no adapter live; runbook sem needle `card nítido; sem grill`; pin Cripto `v1.1.15`.

---

## Audit

| Item da rubrica | Resultado |
| --- | --- |
| Gate parseável `UI impact` / `live_route` / `surface` | PASS — três linhas próprias; `none` / `N/A` / `new`; ausência + justificativa; catálogo não emprestado |
| Bloco D4 colado | PASS — exacto |
| Sem proto / sem HTML | PASS |
| `proposal.md` tem Problema, História, Entra, Não entra com o contrato do issue (não Why de uma linha) | PASS — Problema/História byte-idênticos; Entra/Não entra idênticos salvo delta Q4 autorizado T6 |
| Q1=A Q2=A Q3=B intactos | PASS — texto Entra byte-idêntico ao issue; D2/D3/D4 + specs + tasks alinhados |
| Q4=A substituída: Gist *superset*; golden headings | PASS — D6 + specs grill-card/covenant-flow/cursor-harness + task 5.1 |
| Q1–Q3 não reabertas | PASS |
| Escopo: colunas / T1 / vendor / 1+1+1 / clone viva / produto+pin / zero app | PASS |
| DoD alvo 3; Vocabulário/Riscos no Design; `G_design` pacote+comentário | PASS |
| Entra / não entra não reaberto (salvo Q4 T6) | PASS |
| Classificação P0/P1 vs P3 | PASS — zero furo **novo** de produto/escopo/contrato visível; digest stale / cola crítica 1/1 / needles/tag = P3 |
| Snapshot não vazio | este ficheiro |

Finding determinístico sem classificação: **nenhum**.

P0 **novo** de produto que justificaria segundo rework: **nenhum**.

---

## Achados

- P0 produto/escopo/contrato visível: (nenhum)
- P1 produto/escopo/contrato visível: (nenhum)
- P2 residual aceite (já no `design.md` Risks / Open Questions; **não** reaberto como P0/P1):
  - Detector nítido = heading+texto, não NLU (Problema de uma palavra passa; História em falta após skip = P0 possível na crítica seguinte, Q3=B).
  - Delta PATCH e tecto = skill, não Guard (golden de dump; modelo que reescreve o body inteiro aparece no próximo grill).
  - Copiar história para o Gist vira segunda entrevista → D6: REST → colar; MUST NOT reentrevistar; golden sem as 3 seções falha. (P2 do crítico 1/1 «Gist deixa de ser *superset*» **fechado** por este rework.)
- P3 detalhe de Apply (aceite; resolver no Apply; nunca P0/P1):
  - Retunar `test_alan_workflow_d5_ceiling_sentence` (live asserta `6 seções do DoD`).
  - Pin-tests que cravam `v1.1.15` sobem para a tag deste card.
  - Tag `v1.1.16` ocupada → próximo patch livre; nunca major; `SCHEMA_MAJOR` 1.
  - `.design-digest` ainda hash pré-rework (`4fa18fd5…` ≠ `069a881a…` medido).
  - Seção `## Design Critique` no `design.md` descreve Q4=A / proposal sem recopiar — pai substitui; crítico não edita.

Dual-write lei / reabrir Q1–Q3 / T1 / vendor / 1+1+1 / clone viva / HTML / superfície visual sem classificar / rota de catálogo / código da aplicação: **false**.

---

## Disposition

Zero P0/P1 aberto. Rework 1/1 fecha o furo T6/T7 (Why de uma linha; Gist não-*superset*). `proposal.md` traz o contrato do issue nas quatro seções; Q1=A Q2=A Q3=B intactos; Q4 substituída de forma consistente (D6 + specs + golden). Residuais P2 já nomeados pelo autor (detector heading, PATCH ritual) — aceitos, não reabertos. P3 = needles/tag/pin-tests/digest/cola stale. Tokens parseáveis presentes; Prototype N/A justificado; Entra/não entra do issue não reaberto (salvo delta Q4 autorizado). **Sem segundo rework.**

## Verdict

**PASS** (limpo, pós-rework 1/1)

Prototype: N/A — `UI impact: none`; harness/runbook/kaizen; nenhuma tela CriptoFarol; justificativa não vazia em `## Prototype`.

Tokens: **ok** (`UI impact: none` / `live_route: N/A harness-only; no product route` / `surface: new` em linha própria). Missing: nenhum.

`design.md` **não** editado por este crítico.

Path: `.impeccable/critique/955-card-955-grelha-enxuta-2026-09-16T194042Z.md`
