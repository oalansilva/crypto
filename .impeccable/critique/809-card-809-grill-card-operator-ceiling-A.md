# Snapshot — Assessment A · ronda 2 · card #809 `card-809-grill-card-operator-ceiling`

- Card: #809 — tecto de linguagem no adapter `grill-card` (Qs de operador; identificador do git não é option)
- Change: `card-809-grill-card-operator-ceiling`
- Critic: Assessment A ronda 2 (isolado; inherit de modelo; sem transcript do pai; sem partilha com B; sem nested critic)
- Modelo: inherit
- UTC: 2026-08-30T01:56:00Z
- Tuple (este isolado): hook `bound_card=⊥` `q_git=develop` `q=None`. Write produto deny. Esta onda só `.impeccable/critique/**`. Não T5. Não commit. Não editar `design.md`.
- Board: Project 1 — **Status=Design** (Prioridade P0). Issue OPEN. `UI impact: none` não saltou coluna.
- Digest `design.md` **medido**: sha256 `1b07fe96243df5e2e0eddc11960388206c67fc2c35bd331fe603325b235a3f75` · **1853** palavras (`wc -w`) · 12657 bytes · 149 linhas
- `openspec validate card-809-grill-card-operator-ceiling --type change --strict`: **valid**
- `## Design Critique` / `Design Agent verdict` em `design.md`: **ausentes** (filho autor correto)
- UI impact: **none** (adapter + uma frase no runbook + goldens pytest; nenhuma rota, shell, componente, token ou copy de ecrã do CriptoFarol)
- Prototype: **N/A** — `UI impact: none`; zero HTML `frontend/public/prototypes/*809*`; sem rewrite de `DESIGN.md`; sem pipeline Impeccable visual; Playwright desta coluna = N/A
- Overlay live: `pin: v1.1.5`. Adapter live **sem** tecto (grep `tecto` / `não percebi` / `português de operador` = 0 no canónico). Vendor `grilling` intacto (`❓`/`➡️`; 0 `AskUserQuestion`/`ask_user_question`). Peles `.grok`/`.dsh`/`.opencode` grill-card: 3 linhas não-vazias no body; MUST Read; 0 host tools. Fixtures `grill_ceiling/` **ausentes** (pré-Apply esperado).
- Method: `proposal.md` / `design.md` D1–D9 + Apply contract / Risks / `tasks.md` 1–7; deltas `grill-card` + `covenant-flow`; spec viva `openspec/specs/grill-card/spec.md`; adapter live; vendor; bloco `## Grill-card` de `covenant-flow`; needles `scripts/process-fsm/test_grill_card.py` (read-only). Briefing = artefatos (Qs congeladas no proposal/design + issue #809). Sonda D4: matcher r2 simulado sobre as três frases de operador + asserts de violação.

---

## Brief (só neste snapshot)

Problema: nas Qs de grelha o operador escolhe entre alternativas que só fazem sentido com identificadores do git, e «A» na recomendada ratifica desenho. Outcome: o mesmo tecto em **todo** Em Refinamento (produto + harness); facto no body / *como* no Design; Other vazio / silêncio / «não percebi» reclassificam e nunca aceitam a recomendada; comentário canónico exacto (só o *quando*). Direction: lei no adapter (não no vendor) + uma frase exacta D5 no bloco Grill-card + golden **ambos** (scanner estreito + fixtures, incl. `pass_operator` com as três frases de falso-positivo). Scope: skills/specs/testes de processo. Fora: vendor Matt, colunas/T1/FSM, `#667`/`#755`/`#786`, rewrite de bodies `#795`/`#799`/`#801`, pin major, UI/HTML.

---

## 1. Escopo vs grill Q1=A Q2=A Q3=A

Qs congeladas no issue e no Design; Design não reentrevista. Recorte mapeado:

- **Q1=A** (o mesmo tecto em todo Em Refinamento, produto e harness) → D1 + proposal What + spec `Operator language ceiling on every Em Refinamento card` (cenários product story **e** harness) + tasks 1.1. Alternativa rejeitada (tecto só em harness) não reaparece. Fixture de produto `fail_monitor_path.md` **além** dos três de harness. `pass_operator.md` MAY usar tema Monitor **sem** path/função/flag.
- **Q2=A** (Other vazio / silêncio / «não percebi» / «isto é técnico» reclassificam; **nunca** aceite da recomendada) → D2 + spec `Confusion reclassifies…` (três cenários de stamp; Other do host #755 automático, **não** option listada) + task 1.3 + três fixtures `fail_stamp_*` + frase D5 no runbook. Golden MUST NOT tratar «não percebi» como *label* da option recomendada. Alternativa rejeitada (Other = A / rubber-stamp) está em Non-Goals.
- **Q3=A** (texto canónico exacto; só o *quando*) → D3 + spec `Empty frontier of this adapter is six DoD sections and no open operator decision` + task 1.2. Linha pinada = needle `DOD_NEEDLES` live: `grill-card: fronteira vazia; história no body; à espera de T1 (Alan).` Idempotência permanece. Árvore Matt completa continua no Design; paragem do adapter = 6 seções + zero decisão de operador.

Residuais da grelha fechados no Design, não reabertos como Q: golden = ambos (D4); frase exacta D5; tag esperada `v1.1.6` (D6); peles thin (D7); disparo inalterado (D8). `Open Questions: Nenhuma bloqueante` — coerente.

Não entra (e não foi alargado): vendor `grilling`; `/opsx:explore` como porta; schema `grill-driven`; `CONTEXT.md` / ADR; marketplace; inchar peles; `backend/` / `frontend/src/`; coluna nova; arrastar Status; pin major.

---

## 2. Superfície visual

Nenhuma superfície visual nova ou alterada sem classificação.

- `UI impact: none` com justificativa não vazia (adapter + frase + goldens; zero rota/shell/componente/token/copy de ecrã).
- `## Prototype` / `## Prototype Validation` / pipeline Impeccable desta coluna = N/A justificado.
- Host TUI (`AskUserQuestion` / `ask_user_question`) é vendor — contrato de **conteúdo** das Qs, não patch de UI. Não é ecrã CriptoFarol.
- Apply contract: MUST NOT `backend/**` nem `frontend/src/**`. Task 6.2: zero diff de produto de app.
- Snapshot Impeccable visual N/A no `design.md` (T7 visual). Este ficheiro é o snapshot da crítica de processo.

---

## 3. Regressão (#667 / #755 / #786; vendor intacto)

| Card | Contrato vivo | Este design |
| --- | --- | --- |
| **#667** porta + ledger no issue | Adapter é ficheiro regular; Status=Em Refinamento + N no prompt; DoD no body; proibido T1/`CONTEXT.md`/`/opsx:*`; sem `grill-with-docs` | Non-Goals + spec `Offer-grill trigger stays body without six DoD sections` + «Explore is not the Em Refinamento door». Disparo D8 inalterado. Não reescreve `#795`/`#799`/`#801`. |
| **#755** todas as options | N≥2; todas as alternativas; recomendada primeiro `(Recommended)`; Other não conta; filho dump D5; pai não colapsa; vendor Matt sem nomes de host; stubs Grok sem host tools | Proposal: «Contrato de opções do host intacto». Tasks 1.4 e 4.3 N1 verdes. Spec ceiling: «Host-option ritual … SHALL remain unchanged; only the **content** of questions changes». Frase D5 **além** de `todas as options` / `não colapsa` (task 2.2 MUST manter as duas). Other automático ≠ option listada; stamp dumps não metem «não percebi» em `options[]`. |
| **#786** grelha dsh no root | `## Cliente: dsh` no canónico; `Cliente dsh:` no runbook; root chama `ask_user_question`; stub dsh MUST Read ≤8 | Task 2.2 MUST NOT `dsh_stubs.py`. D7 / task 3.1: peles continuam thin. Frase D5 **antes** de `Cliente dsh:` (não apaga a linha). |

Vendor live medido: `.cursor/skills/grilling/SKILL.md` tem `❓` e `➡️`; 0 hits host tools. Spec `Vendor grilling stays Matt` + task 1.4 + Non-Goals. `grok_stubs.py` / `process-fsm.yaml` / `AGENTS.md` intocados salvo pin que já copia peles. Yaml T1: `from=Em Refinamento` `event=priorizar` `actor=Alan` — sem task de yaml neste card.

Needles `test_grill_card.py` que Apply MUST manter verdes (N1): `DOD_NEEDLES` (inclui o comentário canónico Q3); `disable-model-invocation: false`; sem `grill-with-docs`; host tools no canónico Cursor/Grok; vendor sem host tools; `todas as options` / `não colapsa`; stubs Grok sem host tools; dsh stub ≤8 + MUST Read; pin-test `v1.1.5` **sobe** para a tag deste card (`test_grill_card.py` L256 + `test_dsh_adapter.py` L506/L521).

---

## 4. Golden = scanner **e** fixtures (ambos) — ronda 2

D4 + spec `Golden dumps distinguish ceiling fail from pass` + tasks 4.1–4.2. Apply MUST ter os dois no mesmo `test_grill_card.py`. Só lista = falso negativo nos três dumps de evidência se o scanner for estreito. Só exemplos = o tecto não pega Q nova com path. Esconder `Não priorizar ainda` / `acabada` / `20260830` no `pass_operator.md` **não** basta — o dump MUST contê-las e o scanner MUST devolvê-las falso.

**Matcher r2 (estreito), simulado neste isolado:**

| Texto | Classe r2 | `ceiling_violation` |
| --- | --- | --- |
| `Não priorizar ainda` | evento ≠ substring `priorizar` | **false** (assert pinado) |
| `A história está acabada` / `acabada` | SHA misto exige dígito **e** letra `a–f`; só-letras não casa | **false** |
| `evidência 20260830` | data só-dígitos; MUST NOT `\b[0-9a-f]{7,40}\b` largo | **false** |
| `process_event priorizar` | token `process_event` | **true** |
| `94f8ed41` | 8 hex misto (dígito + `a–f`) | **true** |
| `frontend/src/pages/Monitor.tsx` | path `/.+\.tsx` | **true** (fail_monitor) |
| `WatchlistRow` / `` `WatchlistRow` `` | sem path / sem `_` `()` `.` | false (falso negativo conhecido) |
| `` `priorizar` `` | evento só `process_event`/`iniciar_design` | false (trade-off aceite) |

Fixtures em `scripts/process-fsm/fixtures/grill_ceiling/` (reconstruções; **não** `gh issue edit`):

- fail harness: `fail_795_q2.md`, `fail_799_q3.md`, `fail_801_q3.md`
- fail produto: `fail_monitor_path.md` (path Monitor vs componente)
- pass: `pass_operator.md` (MUST incluir as três frases acima)
- stamp (não label): `fail_stamp_other_empty.md`, `fail_stamp_silence.md`, `fail_stamp_nao_percebi.md`

Equivalente de produto além dos três de harness está nomeado. Golden vive com o adapter (produto + pin). `pytest scripts/process-fsm` sem GitHub.

P1 r1/r2 **fechados no contrato** (não só no Risks): SHA misto + eventos sem token `priorizar`; três asserts de unidade; `pass_operator` obrigado a conter as frases; D5/task 1.3/spec/fixtures de stamp alinhados a Other vazio / silêncio / «não percebi» **nunca** como aceite nem como label `(Recommended)`.

---

## 5. Pin patch, não major; mesmo tecto produto+harness

- Origin live `v1.1.5`. Esperado `v1.1.6`. D6 + spec `Operator ceiling ships as product patch pin` + task 5.1: se ocupada, próximo patch livre; **nunca major**. `SCHEMA_MAJOR` permanece 1 (overlay schema inalterado; canal v1 = copiar e commitar).
- Ordem: commit no produto → tag → `implantar --pin` no Cripto. Overlay `pin:` = essa tag. Rollback = pin `v1.1.5`.
- Pin-tests: `test_grill_card.py` L256 `overlay["pin"] == "v1.1.5"` **e** `test_dsh_adapter.py` (L506 `--pin v1.1.5` + L521) sobem juntos (task 4.3).
- Mesmo tecto produto+harness = Q1=A, não um extra. Spec tem os dois cenários. Fixture de produto é obrigatória, não opcional.

---

## 6. Other / silêncio / «não percebi» nunca a recomendada

Não é aceite. Não é Other = A. Não é silêncio = A. Não é label `(Recommended)`. D2 + spec SHALL (três cenários de comportamento + três de golden stamp) + frase D5 (`Other vazio, silêncio e «não percebi» / «isto é técnico» reclassificam e nunca aceitam a recomendada`) + task 1.3 + task 4.1 proíbe «não percebi» como label. Outras Qs da mesma rodada continuam à espera. Residual aceite no Risks: é texto de adapter, não Guard — modelo que ignora o skill só aparece no próximo grill, não em pytest de needles. Mitigação = três dumps de stamp + needles Q2 no canónico (task 4.2).

---

## 7. Riscos operacionais

Já no `design.md` Risks; nenhum eleva a P0/P1 nesta ronda:

- Scanner estreito vs Q de desenho sem path → rede dos quatro fail dumps. Residual: `WatchlistRow` / `` `priorizar` `` / path `.md`/`.sh`.
- Scanner largo vs Qs de operador → **fechado** no matcher + asserts + `pass_operator` MUST conter as frases.
- Reclassificar = ritual, não Guard.
- Tag `v1.1.6` ocupada entre Design e Apply → bump patch, nunca major.
- DoD já escrito com Qs de desenho no histórico → D8 não re-grelha; tecto daqui para a frente.
- Peles sem o texto do tecto → MUST Read; inchar = regressão do needle ≤8.

Tensão vendor vs adapter (não é furo de recorte): vendor Matt declara sessão concluída quando a árvore **inteira** foi visitada; o adapter pára mais cedo (Q3=A). Spec: MUST NOT keep asking design-tree questions. D9 + task 1.2 alinham passos 4/5. O tecto **não** vai para o vendor (proibido). Apply que «alinhe» o vendor ao *quando* novo seria regressão Matt / #755. Tasks 1.4 e 7.2 cobrem.

T1 continua só Alan. Sem aresta FSM. Sem `enabled_tools` novo.

---

## Achados

- P0: (nenhum)
- P1: (nenhum) — P1 r2 (scanner FP `priorizar`/`acabada`/`20260830`; silent-Other=A / «não percebi» como label) **fechados** em D2/D4/D5 + spec golden + tasks 1.3/4.1/4.2. Sonda do matcher estreito confirma os cinco asserts de unidade.
- P2: Classes do scanner não cobrem todo identificador do git (`WatchlistRow` sem path/backtick-`_`; `` `priorizar` ``; `.md`/`.sh`). Rede = os quatro fail dumps. Já em Risks. Disposition: **accepted-residual**.
- P2: Reclassificar e o tecto são texto de skill, não Guard. Residual aceite no Risks. Disposition: **accepted-residual**.
- P3: Vendor Matt (árvore inteira) vs paragem do adapter (6 seções + zero Q de operador). D9/task 1.2/spec MUST NOT continue design-tree; MUST NOT editar o vendor. Disposition: **accepted-residual**.
- P3: Pytest ≤8 linhas live só no stub dsh; spec SHALL nas três peles. Task 3.1 cobre; 4.2 MAY estender o assert a grok/opencode. Disposition: **accepted-residual**.
- P3: SHA class é `[0-9a-f]` minúsculo; SHA curto 40/7–39 em maiúsculas foge. Rede = fixtures de evidência (hashes live em minúsculas). Disposition: **accepted-residual**.
- Dual-write Hermes/`~/.codex`; reabrir #667/#755/#786; editar vendor; pin major; HTML / superfície visual sem classificar; Other/silêncio/«não percebi» = A ou = label recomendada: **false**.

---

## Disposition

Zero P0/P1. Recorte Q1=A Q2=A Q3=A congelado; Non-Goals não alargados. Golden ambos (scanner estreito + oito fixtures: quatro fail de identificador, um pass com as três frases, três stamp). Pin patch `v1.1.6` (ou próximo patch; nunca major). Other vazio / silêncio / «não percebi» nunca aceitam a recomendada e nunca são label `(Recommended)`. Vendor Matt e ritual #755 intactos. `UI impact: none` sem superfície visual por classificar. Apply contract executável após Pronto para Dev. Residuais P2/P3 são limites conhecidos do scanner/ritual/vendor — não bloqueiam.

## Verdict

**PASS**

Prototype: N/A — `UI impact: none`; adapter `grill-card` + frase `covenant-flow` + goldens; nenhuma tela CriptoFarol.
Snapshot: `.impeccable/critique/809-card-809-grill-card-operator-ceiling-A.md`
