# Release 2026-07-31d - Carteira: stables Spot + Simple Earn

## Objetivo

Publicar em produção a correção homologada da Carteira para exibir saldos USDT/USDC (e demais stables) alinhados à Vista de ativos da Binance, incluindo Simple Earn Flexible.

## Cards incluídos

- `#346` Bug: USDC e USDT não aparecem na Carteira (Homologado).

## Entregáveis

- Fallback de preço ~1 USD para stables (Spot e wrappers `LD*`) sem omitir row por ticker ausente.
- UI da Carteira propaga `min_usd` até a API de balances.
- Snapshot prefere `Simple Earn` flexible/locked sobre `LD*` incompleto no `/api/v3/account`; fallback para `LD*` se Earn falhar.
- Total da Carteira alinhado à Vista de ativos (~Δ residual só de preço/timing).

## OpenSpec

- Change arquivada: `card-346-wallet-stable-balances-display`.
- Spec sincronizado: `external-balances`.

## Evidência esperada

- `openspec validate --all`
- `scripts/release-guard pre`
- PR `develop -> main`, CI verde e merge manual
- atualização e restart dos serviços afetados em PROD
- validação pública de `https://criptofarol.com.br`
- `scripts/release-guard post`
- card `#346` movido para `Pronto`
