# Release 2026-08-28

Doc canônica do dia 2026-08-28.

## Lote — cards 749 e 752

Cards `Homologado` incluídos (Alan em chat: `suba a release` com Homologado anterior):

- `#749` (Monitor, P0, Codex) — P0: Perfil Telegram — reload mostra Não vinculado apesar do vínculo salvo (#747) — defense in depth (preservar stale `hasLoadedRef` + `isRealLogout`, `AbortController`/`cancelled`, `onSettingsChangeRef`, `ProfilePage` guard `authLoading`)
- `#752` (Operacao, P0, Kaizen, Codex) — bootstrap .env PROD append-only (nunca sobrescrever o arquivo) — `ops/bootstrap_env.py` fail-closed, append-only por chave, piso `DATABASE_URL`+`JWT_SECRET` imutável, backup timestamped, replace atómico

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`).

Responsáveis Clara/Alan: nenhum neste lote (ambos `Responsavel=Codex`). Copy de landing inalterada. `UI impact: none` (749 é correção de estado, sem protótipo; 752 é `ops/` + doc).

### OpenSpec

- Changes arquivadas: `card-749-telegram-reload` → `openspec/changes/archive/2026-08-28-card-749-telegram-reload/` e `card-752-bootstrap-env-append-only` → `openspec/changes/archive/2026-08-28-card-752-bootstrap-env-append-only/` (commit docs-release).
- Main specs: `openspec/specs/user-telegram-alerts/spec.md` (update +1 ADDED Requirement: reload `Vinculado` + 401 transitório vs real + sync Monitor) e `openspec/specs/bootstrap-env-append-only/spec.md` (create +6 Requirements fail-closed). `openspec archive -y` com sync de specs.
- `openspec validate --all`: 160/160 no archive; 160/160 em `docs-release` branch; CI do PR #768 verde.

### Git e publicação

- `origin/develop` continha o pacote homologado (PR #767 `37836e58` para 749 em cima de `22eec442` para 752, ambos Homologado com comentário canônico `01:10:48Z`/`01:10:50Z` via `post-card-evidence-comment.sh --transition homologado`).
- `release-guard pre`: PASS (`RELEASE_CARDS=749,752`; `PRESERVED_BRANCHES` 7 in-flight + `RELEASE_BRANCHES` 2 do pacote; `RELEASE_DATE=2026-08-28`). Homologado comments verificados antes do `pre`.
- PR `develop -> main` #768, merge `2478bdee` (`Merge pull request #768 from oalansilva/develop`).
- CI do PR #768: 13 checks `pass` (`backend-tests`, `frontend-tests`, `e2e-playwright` 4m53s, `openspec-validate`, `process-fsm`, `changes`, etc.); `qa-gate`/`deploy-staging` `skipping` (base=main, esperado para release).
- Docs PR: `docs-release-2026-08-28 -> main` (este commit) com `docs/release-2026-08-28.md` + `docs/kaizen-log.md` + archives 749/752 + specs.
- Sync `main → develop`: PR `sync-2026-08-28` merge para árvores idênticas (após docs merge, reexecutar `release-guard post` se diverge).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `2478bdee` (merge PR #768).
- Migration: `alembic upgrade head` (PostgresqlImpl, já head; sem migration nova para 749/752 — Telegram já via `crypto_app` e bootstrap é `ops/`).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CCFZ7r0d.js` / `index-DQ-0-Hba.css` (1,773.21 kB / 212.62 kB, gzip 538.46 kB / 33.58 kB).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200.

### Evidência de deploy

`2478bdee services=criptofarol-prod-backend,criptofarol-prod-frontend url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-28, com os cards ainda Homologado. Card kaizen `#769` (tasks.md `[ ]` antes de Done) materializado; F-2 coberto por #659, F-4 por #759, F-5 por #658, F-3 por gate documental.

## Lote — card 773

Cards `Homologado` incluídos (Alan em chat: `suba a release` + `agora movi para homologado`):

- `#773` (Produto, kaizen no board Title legado) — pin Cripto a `covenant-flow` v1.0.1: skills `covenant-flow` / `covenant-flow-environments` / `implantar`, overlay `.covenant-flow/overlay.yaml`, Guard/overlay-reading, remoção de `BUGBOT.md`, review em `diff-reviewer`+`code-reviewer`. `UI impact: none`. Troca viva do **host** (`~/.cursor/skills/alan-workflow*` → `covenant-flow*`) **fora** deste merge; só após `#773` = Pronto.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`). Worktree `card-773-covenant-flow` classificada no `pre` (críticas Impeccable untracked) e apagada no closeout.

Responsáveis Clara/Alan: nenhum neste lote. Copy de landing inalterada. `UI impact: none`.

### OpenSpec

- Change arquivada: `card-773-covenant-flow` → `openspec/changes/archive/2026-08-28-card-773-covenant-flow/` (`openspec archive -y`, sync de specs).
- Main specs: create `openspec/specs/covenant-flow/spec.md` (+11 requirements); update cursor-code-review, cursor-harness, developer-tooling (remove BUGBOT.md / add local reviewers), oracle-environment-map, process-fsm, process-fsm-event, process-fsm-guard, process-fsm-paging, process-harness, release-archive-via-release-branch.
- `openspec validate --all`: 160/160 após o archive.

### Git e publicação

- `origin/develop` continha o pacote homologado (PR #778 squash `65d5bd74`).
- `release-guard pre`: PASS (`RELEASE_CARDS=773`; `PRESERVED_BRANCHES` 7 in-flight + `card-773-covenant-flow`; `RELEASE_DATE=2026-08-28`). Comentário Homologado canónico presente (`#issuecomment-5454841606`).
- PR `develop -> main` #779, merge `97ff2de6` (`Merge pull request #779 from oalansilva/develop`). Árvores `develop`/`main` idênticas após o merge (`1ba6e9a2`).
- CI do PR #779: checks `pass` (`backend-tests`, `frontend-tests`, `e2e-playwright` 4m38s, `openspec-validate`, `process-fsm`); `qa-gate`/`deploy-staging` `skipping` (base=`main`).
- Docs PR: `docs-release-2026-08-28-773 -> main` (este commit) actualiza a **mesma** `docs/release-2026-08-28.md` + `docs/kaizen-log.md` + archive 773 + specs. Não reutiliza `PROD_DEPLOY_EVIDENCE` do lote 749+752.

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `97ff2de6` (merge PR #779).
- Migration: `alembic upgrade head` (PostgresqlImpl, já head; pin de harness sem migration nova).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CCFZ7r0d.js` / `index-DQ-0-Hba.css` (1,773.21 kB / 212.62 kB, gzip 538.46 kB / 33.58 kB) — hashes iguais ao lote 749+752 (sem mudança de `frontend/src`).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200.

### Evidência de deploy

`97ff2de6 services=criptofarol-prod-backend,criptofarol-prod-frontend url=https://criptofarol.com.br`

### Kaizen

Entrada canónica na mesma data 2026-08-28 (`### Lote 773`). Card kaizen `#780` (Project Title vs issue title); F-8 coberto por #659; F-9 coberto por #759; F-10 por #658.
