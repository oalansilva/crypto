# Release 2026-09-08

Doc canônica do dia 2026-09-08.

## Lote — cards 821, 837, 850, 852, 853, 854, 858, 859

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#821` (Operacao, P1, Clara) — dsh unbound não pode deny T16 por causa da página Moore (pedido explícito carrega overlay + environments e segue T16). `UI impact: none`. OpenSpec `card-821-dsh-unbound-t16-moore`. PR #866 `e2f72f49`. Residual 7.1 (dump `:3080` DoD humano) permanece `[ ]`.
- `#837` (Produto, P0, Clara) — Descoberta Iniciar começa a varredura da seleção atual (pós-terminal começa nova, sem 409; em execução exige cancelar; sem duplicata; erro em linguagem de operação). `UI impact: affected` (DiscoveryPage). OpenSpec `card-837-descoberta-iniciar-varredura`. PR #851 `b65f6649`. Residual tasks 1.1/2.1–2.4 (sync de spec feito no archive) permanece `[ ]` no archive.
- `#850` (Operacao, P0, Alan) — pin do Guard dsh recarrega o isolate vivo `:3080` (bounce após pin; primeiro turno sem `none`; filho com `stopReason` + `Diagnostic:`). `UI impact: none`. OpenSpec `card-850-dsh-isolate-reload`. PR #865 `07ed8667`. Residual 7.2 (dumps `:3080`) permanece `[ ]`.
- `#852` (Produto, P0, Clara) — Descoberta em 3 modos (Montar / Acompanhar / Decidir; preflight 3 linhas; leaderboard 1 ordenação + 3 colunas risco; seleção inline; Iniciar dominante; promoção com resumo lado a lado). `UI impact: affected`. OpenSpec `card-852-descoberta-tres-modos`. PRs #857 `935db1e8` + #863 `4a003481` (T18 edição avançada fiel ao protótipo). Residual tasks 1.1–3.x permanece `[ ]` no archive.
- `#853` (Produto, P0, Clara) — cancelamento de varredura auto-finaliza sem depender de worker (sem nada em execução conclui na hora; com avaliações em voo aguarda e conclui; F5 e nova varredura funcionam). `UI impact: none` (comportamento). OpenSpec `card-853-cancel-reconcile`. PR #855 `3552c923`. Sem residual `[ ]`.
- `#854` (Operacao, P1, Alan) — Design fecha em 1+1+1 com gate no autor (classificação P0/P1 vs P3; live_route/surface/COPIED no primeiro autor). `UI impact: none` (harness). OpenSpec `card-854-design-teto-gate`. PR #856 `267e75de`. Residual tasks 1.1–3.1 permanece `[ ]` no archive.
- `#858` (Operacao, P0, Alan) — dsh `:3080` não fica idle no closeout (espera no turno; wake sem continue; um reenvio em turno morto). `UI impact: none`. OpenSpec `card-858-dsh-idle-closeout`. PR #862 `8d431f62`. Residual 8.1 (dump `:3080` closeout QA) permanece `[ ]`.
- `#859` (Operacao, P1, Alan) — ao homologar, Alan pode não aprovar e devolver o mesmo card (`nao_homologar` T18 com motivo `Não homologar:`). `UI impact: none`. OpenSpec `card-859-devolver-homologacao`. PR #860 `2cb33342`. Sem residual `[ ]`.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`); branch remota órfã `origin/card-469-idempotency-normalization-wip` (WIP preservado fora do lote desde 2026-08-17) preservada no `post`. `#868` kaizen nascido neste closeout em Em Refinamento.

Responsáveis Clara: `#821` `#837` `#852` `#853`. Responsáveis Alan: `#850` `#854` `#858` `#859`. Copy de landing **inalterada**. Pin overlay `v1.1.14`.

### OpenSpec

- Changes arquivadas: `card-821-dsh-unbound-t16-moore` → `openspec/changes/archive/2026-09-08-card-821-dsh-unbound-t16-moore/`; `card-837-descoberta-iniciar-varredura` → `openspec/changes/archive/2026-09-08-card-837-descoberta-iniciar-varredura/`; `card-850-dsh-isolate-reload` → `openspec/changes/archive/2026-09-08-card-850-dsh-isolate-reload/`; `card-852-descoberta-tres-modos` → `openspec/changes/archive/2026-09-08-card-852-descoberta-tres-modos/` (spec nova `discovery-three-modes` criada); `card-853-cancel-reconcile` → `openspec/changes/archive/2026-09-08-card-853-cancel-reconcile/`; `card-854-design-teto-gate` → `openspec/changes/archive/2026-09-08-card-854-design-teto-gate/`; `card-858-dsh-idle-closeout` → `openspec/changes/archive/2026-09-08-card-858-dsh-idle-closeout/`; `card-859-devolver-homologacao` → `openspec/changes/archive/2026-09-08-card-859-devolver-homologacao/` (todas com `openspec archive -y`, specs sincronizadas sem `--skip-specs`; tasks residuais `[ ]` documentadas acima).
- Main specs atualizadas: `covenant-flow`, `process-fsm-paging`, `discovery-sweep` (2×), `developer-tooling`, `process-harness` (2×), `discovery-three-modes` (nova), `design-route-clone-gate`, `llm-flow-emission`, `process-fsm`, `process-fsm-event`.
- `openspec validate --all`: 170/170 no `pre` (8 changes ativas); 163/163 após archive.
- Ativas restantes: nenhuma.

### Git e publicação

- `origin/develop` `e2f72f49` continha só o pacote homologado (`#851` `#855` `#856` `#857` `#860` `#862` `#863` `#865` `#866`).
- `release-guard pre` PASS (`RELEASE_CARDS=821,837,850,852,853,854,858,859`; `PRESERVED_BRANCHES` 10 in-flight/leftover; `RELEASE_DATE=2026-09-08`). Comentários Homologado canónicos dos oito cards postados neste turno antes do `pre`. Campos Responsável/Prioridade/Tipo preenchidos neste turno (`#850` Responsável Alan; `#852`/`#853` Clara/P0/Produto).
- PR `develop -> main` #867, merge `505e3b0dbfb1b6f23ba09599cb569dd19294e443` (`Merge pull request #867 from oalansilva/develop`).
- CI do PR #867: checks `pass` (`backend-tests` 2m5s, `backend-unit-tests` 1m55s, `e2e-playwright` 5m5s, `openspec-validate` 17s, `process-fsm` 45s, `frontend-build` 38s, `frontend-lint`, `frontend-tests`, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`, `changes`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 867 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-08 -> main` (este commit) com `docs/release-2026-09-08.md` + `docs/kaizen-log.md` + 8 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (root-owned; operações com `sudo`).
- Commit publicado: `505e3b0dbfb1b6f23ba09599cb569dd19294e443` (merge PR #867).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl, já head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-BV0bshgg.js` / `index-C1utNfgZ.css` (1,779.76 kB / 211.36 kB, gzip 540.59 kB / 33.37 kB). Hash JS novo vs lote 2026-09-05 (`index-DNi9d2DD.js`) pelas mudanças Descoberta #852/#837/#853.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` (`criptofarol-prod-candle-writer.service` timer, inactive). Health 200.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200.

### Evidência de deploy

`505e3b0dbfb1b6f23ba09599cb569dd19294e443 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-08 (subsecção lote 821/837/850/852/853/854/858/859), com os cards ainda Homologado. Card novo `#868` (source PROD root-owned + dist root-owned). Dedupe `#658` `#769` `#759` `#825`.

### Drive

Sincronização Google Drive da Clara **não executada** neste lote: sem documento de produto alterado (sem mudança em `docs/mvp-scope.md`/`decision-log.md`/landing copy; Descoberta #852/#837/#853 são refinamento de fluxo e bugfix com specs versionadas no git). Recidiva `#825` cobre eventual pendência de credencial.
