# Release 2026-08-20 — Lotes 605/610/611 e 612

Doc canônica do dia 2026-08-20. Dois pacotes no mesmo dia; este arquivo é atualizado após cada deploy.

## Lote — card 612

Cards `Homologado` incluídos (Alan em chat: `esta homologado suba a release`):

- `#612` (P1, Operacao) — Harness: process_event (SMAG; T8/T17). Resp: Clara.

Fora do lote: `#608`/`#613` Em Refinamento (epic/paging). `#614` Aprovação de Design (artigo). `#472`/`#606` in-flight. Kaizen novos `#631`/`#632` em Em Refinamento; `#617`/`#625` reusados.

Responsáveis Clara/Alan: `#612` (Clara). Documentação atualizada neste closeout (`decision-log`, `kaizen-log`, `project-hub`). Copy de landing inalterada.

### OpenSpec

- Change arquivada em 2026-08-20: `card-612-process-event` → `openspec/changes/archive/2026-08-20-card-612-process-event/`.
- Spec nova: `process-fsm-event`.
- Sync em specs existentes: `process-fsm` (guardas nomeadas + I4), `process-fsm-guard` (deny Status item-edit e sidecar), `cursor-harness` (Agent só via process_event).
- CLI archive com `--skip-specs` após sync agent-driven.
- `openspec validate --all`: 148/148 (CI PR #630).

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `f6388539`, PR #629).
- Branch `release-2026-08-20-612` = `f6388539` + archive `7c99a894`.
- `release-guard pre`: PASS (`RELEASE_CARDS=612`; `PRESERVED_BRANCHES` para in-flight #472/#606/#614/#584 e leftovers #612).
- Comentário Homologado canônico no #612 antes do `pre`.
- PR `release-2026-08-20-612 -> main` #630, merge `5110b9b0`.
- CI do PR #630: checks iniciados verdes; `qa-gate` e `deploy-staging` skip no PR para `main` (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `5110b9b0` (`git fetch origin && git reset --hard origin/main`; merge PR #630).
- Migration: `alembic upgrade head` (já era head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-A5R04R0w.js` / `index-Cyx126Ee.css`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-20 (lote 612), com o card ainda Homologado. Cards kaizen #631 e #632 criados em Em Refinamento; F-3 reusa #617.

## Lote — cards 605, 610, 611

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#605` (P1, Codigo) — Discovery: sempre usar Deep Backtest 15m + walk-forward 70/30. Resp: Clara.
- `#610` (P0, Operacao) — Harness: resolver `(q, bound_card, q_git)` pelo worktree do path. Resp: Clara.
- `#611` (P0, Operacao) — Harness: Guard Write (`preToolUse` compilado da tabela). Resp: Clara. Absorve o #606 Cancelado.

Fora do lote: `#608`/`#612`/`#613` Em Refinamento. `#614` Aprovação de Design (artigo). `#609` já Pronto na release 2026-08-19.

Responsáveis Clara/Alan: `#605`, `#610`, `#611` (Clara). Documentação atualizada neste closeout (`decision-log`, `kaizen-log`, `project-hub`). Copy de landing inalterada.

### OpenSpec

- Changes arquivadas em 2026-08-20: `discovery-walk-forward-split`, `card-610-process-fsm-resolver`, `card-611-process-fsm-guard`.
- Specs novas: `process-fsm-resolver`, `process-fsm-guard`.
- Sync em specs existentes: `discovery-sweep` (Deep 15m + split 70/30), `discovery-leaderboard` (ranking IS + evidência OOS), `cursor-harness` (hook Guard), `process-fsm` (purpose).
- CLI archive com `--skip-specs` após sync agent-driven.
- `openspec validate --all`: 147/147.
- Task 3.2 do #605 (smoke DEV pós-merge) ficou `[ ]` no archive; evidência de runtime ficou no deploy PROD (workers Discovery reiniciados).

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `e78d358c`) mais o código dos 3 cards. Archive não foi para `develop` (proteção `qa-gate`).
- Branch `release-2026-08-20` = `e78d358c` + archive `06836f77`.
- `release-guard pre`: PASS (`RELEASE_CARDS=605,610,611`; `PRESERVED_BRANCHES` para in-flight #472/#606/#614/#584 e leftovers #605/#610/#611).
- Comentários Homologado canônicos nos 3 cards antes do `pre` (#610/#611 já existiam; #605 postado neste turno).
- PR `release-2026-08-20 -> main` #624, merge `ad8158fd`.
- CI do PR #624: checks iniciados verdes; `qa-gate` e `deploy-staging` skip no PR para `main` (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `ad8158fd` (`git fetch origin && git reset --hard origin/main`; merge PR #624).
- Migration: `alembic upgrade head` → `20260818_0001` (já era head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-A5R04R0w.js` / `index-Cyx126Ee.css` servido em `/login`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites`, `/discovery`, `/combo` → 200.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-20, com cards ainda Homologado. Card kaizen #625 (P1) criado em Em Refinamento; F-2 (Homologado sem comentário no #605) é recidiva do #579.
