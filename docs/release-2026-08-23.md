# Release 2026-08-23

Doc canônica do dia 2026-08-23.

## Lote — cards 661 / 663 / 664 / 667 / 668

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#661` (P1, Operacao, Clara) — kaizen: `release-guard post` exige materialização de cards Em Refinamento ou dedupe auditável
- `#663` (P1, Produto, Codex) — Discovery: promover ou excluir resultado da varredura no leaderboard
- `#664` (P1, Produto, Codex) — Discovery: restaurar varredura ativa após reload e não perder o progresso
- `#667` (P2, Operacao, Clara) — Processo: grelhar a história em Em Refinamento; OpenSpec só sintetiza em Design
- `#668` (P2, Operacao, Clara) — Harness: adapters Cursor e Grok Build sobre o mesmo núcleo

Fora do lote: `#600` / `#614` Aprovação de Design; kaizen `#658` / `#659` / `#660` Em Refinamento. Leftovers `#472` / `#604` / `#606`.

Responsáveis Clara/Alan: `#661` `#667` `#668` (Clara). Documentação atualizada neste closeout (`decision-log`, `kaizen-log`, `project-hub`, `backlog-operating-model`). Copy de landing inalterada.

### OpenSpec

- Changes arquivadas em 2026-08-23:
  - `card-661-kaizen-release-guard-cards` → `openspec/changes/archive/2026-08-23-card-661-kaizen-release-guard-cards/`
  - `card-663-discovery-discard` → `openspec/changes/archive/2026-08-23-card-663-discovery-discard/`
  - `card-664-discovery-restore-reload` → `openspec/changes/archive/2026-08-23-card-664-discovery-restore-reload/`
  - `card-667-grill-card` → `openspec/changes/archive/2026-08-23-card-667-grill-card/`
  - `card-668-multi-harness-adapters` → `openspec/changes/archive/2026-08-23-card-668-multi-harness-adapters/`
- `#661` `#663` `#664` `#668`: archive `--skip-specs` (headers ADDED já no main spec / MODIFIED divergente). Justificativa operacional (#659).
- `#667`: sync — spec nova `grill-card` e deltas `cursor-harness` / `process-fsm`.
- `openspec validate --all`: 153/153 (CI PR #671).

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `c88ba0a3`).
- Branch `release-2026-08-23` = `c88ba0a3` + archive `b3373491`.
- `release-guard pre`: PASS (`RELEASE_CARDS=661,663,664,667,668`; `PRESERVED_BRANCHES` in-flight + leftovers).
- Comentários Homologado canônicos postados neste turno (antes do `pre`).
- PR `release-2026-08-23 -> main` #671, merge `aa36deb1`.
- CI do PR #671: checks iniciados verdes; `qa-gate` e `deploy-staging` skip (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `aa36deb1` (pacote; merge PR #671). Closeout hotfix `478f0398` (PR #675: tempfile no checker Kaizen; source PROD realinhado).
- Migration: `alembic upgrade head` → `20260821_0001 -> 20260823_0001` (unicidade da outbox Discovery `(sweep_id, generation)`).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-Dtfnr-Df.js` / `index-DzSLxG6d.css`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/monitor`, `/discovery`, `/combo` → 200. Interno `127.0.0.1:8003/api/health` e `:5173/` → 200 após warmup. Health público 502 no primeiro segundo após o restart; 200 na segunda tentativa.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-23 (este lote), com os cards ainda Homologado. Sem card kaizen novo; fricções cobertas por `#658` e `#659` (Em Refinamento). Closeout: `release-guard post` exigiu hotfix `478f0398` (PR #675) porque o snapshot do board no env estourava ARG_MAX no checker do #661.

### Closeout hotfix

- PR #675 `fix/kaizen-board-json-file` → `main` merge `478f0398`.
- Source PROD: `478f0398`. Health `https://criptofarol.com.br/api/health` → 200. Bundle do pacote inalterado (`index-Dtfnr-Df.js`).

## Lote — card 673

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#673` (P1, Operacao, Clara) — kaizen: cortar output LLM no fluxo (HTML/crítica/`design.md`/sessão) sem perder gates

Fora do lote: `#600` / `#614` Aprovação de Design; kaizen `#658` / `#659` / `#660` Em Refinamento. Leftovers `#472` / `#604` / `#606`.

Responsáveis Clara/Alan: `#673` (Clara). Documentação atualizada neste closeout (`decision-log`, `kaizen-log`, `project-hub`, `backlog-operating-model`). Copy de landing inalterada.

### OpenSpec

- Change arquivada em 2026-08-23: `card-673-cut-llm-output-keep-gates` → `openspec/changes/archive/2026-08-23-card-673-cut-llm-output-keep-gates/`.
- Archive `--skip-specs` (headers ADDED já no main spec após o apply). Justificativa operacional (#659).
- `openspec validate --all`: 154/154 (CI PR #680).

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `9a59dddd`, PR #679).
- Branch `release-2026-08-23` = `9a59dddd` + archive `dadd739d`.
- `release-guard pre`: PASS (`RELEASE_CARDS=673`; `PRESERVED_BRANCHES` in-flight + leftovers).
- Comentário Homologado canônico postado neste turno (antes do `pre`). Campo `Responsável=Clara` preenchido neste turno.
- PR `release-2026-08-23 -> main` #680, merge `36534ae1`.
- CI do PR #680: checks iniciados verdes; `qa-gate` e `deploy-staging` skip (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `36534ae1` (pacote; merge PR #680).
- Migration: `alembic upgrade head` (já head; sem migration nova neste lote harness).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-Dtfnr-Df.js` / `index-DzSLxG6d.css` (inalterado vs lote anterior).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/monitor`, `/discovery`, `/combo` → 200. Interno `127.0.0.1:8003/api/health` e `:5173/` → 200 após warmup. Health público 502 nas duas primeiras tentativas após o restart; 200 na terceira.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-23 (este lote), com o card ainda Homologado. Sem card kaizen novo; fricções cobertas por `#658` e `#659` (Em Refinamento).
