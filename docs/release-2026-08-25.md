# Release 2026-08-25

Doc canônica do dia 2026-08-25.

## Lote — card 684

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#684` (P0, Seguranca, Codex) — recusar `JWT_SECRET` default/fraco no runtime que assina/valida JWT

Fora do lote: `#600` / `#614` Aprovação de Design; `#604` Pronto para Dev; kaizen `#658` / `#659` / `#660` Em Refinamento. Leftovers `#472` / `#606` Cancelado.

Responsáveis Clara/Alan: nenhum neste lote. Copy de landing inalterada.

### OpenSpec

- Change arquivada em 2026-08-25: `card-684-jwt-secret-fail-closed` → `openspec/changes/archive/2026-08-25-card-684-jwt-secret-fail-closed/`.
- Main spec sincronizada: `openspec/specs/jwt-secret-fail-closed/spec.md`.
- Archive `--skip-specs` depois do sync (headers ADDED já no main spec). Justificativa operacional (#659).
- `openspec validate --all`: 155/155 (CI PR #721).
- Task 4.1 operacional (rotação DEV no Done; PROD neste release) ficou `[ ]` no archive; evidência no Done/release, sem o valor.

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `3ce00bd7`, PR #719).
- Branch `release-2026-08-25` = `3ce00bd7` + archive `bcd0c400`.
- `release-guard pre`: PASS (`RELEASE_CARDS=684`; `PRESERVED_BRANCHES` in-flight + leftovers).
- Comentário Homologado canônico e `Responsável=Codex` preenchidos neste turno (antes do `pre`).
- PR `release-2026-08-25 -> main` #721, merge `7af54584`.
- CI do PR #721: checks iniciados verdes; `e2e-playwright` pass; `qa-gate` e `deploy-staging` skip (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `7af54584` (pacote; merge PR #721).
- Migration: `alembic upgrade head` (já head; sem migration nova).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-Dtfnr-Df.js` / `index-DzSLxG6d.css` (inalterado vs lote 2026-08-23).
- Rotação `JWT_SECRET` no `.env` PROD (fora do git): chave ausente → gerada (len 64, ≠ default). Valor não entrou em git, unit `Environment=`, chat, issue ou evidência.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login` → 200. Interno `127.0.0.1:8003/api/health` → 200 após warmup. Health público 502 no primeiro segundo após o restart; 200 na tentativa seguinte.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-25, com o card ainda Homologado. Sem card kaizen novo; fricções cobertas por `#658` e `#659` (Em Refinamento).

## Lote — card 685

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#685` (P0, Seguranca, Codex) — `GET /api/workflow/projects` admin-only; DTO sem `database_url` / `workflow_database_url` / `root_directory`

Fora do lote: worktrees in-flight `#600` / `#614` / `#604` / leftovers `#472` / `#606`; kaizen `#658` / `#659` / `#660` Em Refinamento.

Responsáveis Clara/Alan: nenhum neste lote. Copy de landing inalterada. Rotação das URLs já vazadas continua fora de escopo.

### OpenSpec

- Change arquivada em 2026-08-25: `card-685-workflow-projects-lockdown` → `openspec/changes/archive/2026-08-25-card-685-workflow-projects-lockdown/`.
- Main spec sincronizada: `openspec/specs/workflow-projects-lockdown/spec.md`.
- Archive `--skip-specs` depois do sync do main spec. Justificativa operacional (#659).
- `openspec validate --all`: 156/156 nesta branch.

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `da77e93e`, PR #725).
- Branch `release-2026-08-25-685` = `da77e93e` + archive/spec/docs deste lote.
- Comentário Homologado canônico e `Responsável=Codex` preenchidos neste turno (antes do `pre`).
- PR `release-2026-08-25-685 -> main` aberto neste closeout; SHA de merge e CI ficam na atualização documental pós-merge.

### Deploy PROD

Preenchido após merge em `main` e deploy no source PROD (mesmo closeout).

### Kaizen

Entrada `## 2026-08-25 — Kaizen release (lote 685)` em `docs/kaizen-log.md`, card ainda Homologado. Sem card kaizen novo; F-1/F-2 cobertos por `#658` e `#659`.
