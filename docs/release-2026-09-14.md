# Release 2026-09-14

Doc canônica do dia 2026-09-14.

## Lote — cards 904, 921, 927, 935

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#904` (Operacao, P1, Alan) — modelo por papel no Cursor: juízo Grok 4.6, execução Composer 2.5; filho `fecho-lote`; D10 recusa T16 em pai Cursor ≠ Composer. `UI impact: none`. OpenSpec `card-904-cursor-modelo-por-papel`. PRs originais #905/`38b854a3` + T18 #918/`cf8f49a0`. Archive `--skip-specs`.
- `#921` (Produto, P0, Clara, Dados) — velas e médias alinhadas até a última vela carregada na análise e no Monitor; writer PROD passa a 15m/1h/4h/1d. `UI impact: affected`. OpenSpec `card-921-velas-em-falta-analise`. PRs #932/`8689a71c` + T18 #934/`a7f145a3`.
- `#927` (Operacao, P0, Clara, kaizen) — Caso A de release fecha sem stall (`post` main⊆develop, preflight tsc no `release-*`, T16 no git certo). `UI impact: none`. OpenSpec `card-927-caso-a-release-sem-stall`. PR #933/`d4b2a42d` + preflight cwd `c1493106`.
- `#935` (Produto, P0, Alan, Backtest) — retorno composto canónico igual em Favoritos, resumo da análise e Ver Trades. `UI impact: affected`. OpenSpec `card-935-favorites-combo-return`. PR #936/`23369ac9` + testids `09bc7c5a` + snapshot Descoberta `27cb48a9`.

Fora do lote: `#897` `#899` Pronto no board com código ainda na `develop` (restore pós-#916/#917); `#600` `#614` `#728` em Aprovação de Design. Caminho `release-*` a partir de `main` + cherry-picks, não `develop → main`. Worktrees in-flight preservadas via `PRESERVED_BRANCHES`. Kaizen nascidos neste closeout: `#938` `#939`. Dedupe `#909` `#910` `#658` `#759` `#888` `#868`.

Responsáveis Clara: `#921` `#927`. Responsáveis Alan: `#904` `#935`. Copy de landing **inalterada**. Pin overlay `v1.1.15`.

### OpenSpec

- Changes arquivadas: `card-904-cursor-modelo-por-papel` → `openspec/changes/archive/2026-09-14-card-904-cursor-modelo-por-papel/` (`--skip-specs`); `card-921-velas-em-falta-analise` → `…/2026-09-14-card-921-velas-em-falta-analise/` (spec nova `aligned-candle-ma-series`); `card-927-caso-a-release-sem-stall` → `…/2026-09-14-card-927-caso-a-release-sem-stall/`; `card-935-favorites-combo-return` → `…/2026-09-14-card-935-favorites-combo-return/` (spec nova `favorites-combo-return-parity`).
- Main specs atualizadas: `aligned-candle-ma-series` (nova), `favorites-combo-return-parity` (nova), `chart-visualization`, `ci-testing-hardening`, `favorites`, `market-candles`, `monitor`, `process-fsm-event`, `release-archive-via-release-branch`, `release-worktree-hygiene`. Specs `#904` já estavam no main spec.
- `openspec validate --all`: 169/169 na `release-2026-09-14` após archive (173 no `develop` com leftover `#897`/`#899`).
- Ativas nesta branch de release: nenhuma. Na `develop` permanecem as changes dos leftovers `#897` `#899`.

### Git e publicação

- `origin/develop` `aced62e1` continha o pacote **e** leftover `#897`/`#899`. Pacote isolado em `release-2026-09-14` (base `origin/main` `4f071c38` + cherry-picks). Conflito `#935` em `FavoritesDashboard.tsx` resolvido sem puxar promoção `#897`.
- `release-guard pre` PASS (`RELEASE_CARDS=904,921,927,935`; `PRESERVED_BRANCHES` in-flight + leftover + `wip-board-prototype`; `RELEASE_DATE=2026-09-14`). Comentários Homologado canónicos dos quatro cards postados neste turno antes do `pre`. Campos `#904` Alan/P1/Operacao/Operacao preenchidos neste turno.
- PR `release-2026-09-14 → main` #937, merge `1e90437008aafe0a7fde39a4a3a9d6c298a08e93` (`Merge pull request #937 from oalansilva/release-2026-09-14`).
- CI do PR #937: SHA `c1493106` e2e fail (testid `favorite-935`); SHA `09bc7c5a` e2e fail (`favorite-193` return `"-"`); SHA `27cb48a9` checks `pass` (`e2e-playwright` 5m51s, `backend-tests` 1m49s, `backend-unit-tests` 2m13s, `openspec-validate` 21s, `process-fsm` 49s, `frontend-build` 48s) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 937 --watch --fail-fast --interval 20` verde no SHA final. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-14 → main` (este commit) com `docs/release-2026-09-14.md` + `docs/kaizen-log.md` + `docs/mvp-scope.md` + `docs/decision-log.md`.
- Sync `main → develop`: obrigatório após docs merge (Caso A 5b; extra na develop = aviso).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (root-owned; operações com `sudo`).
- Commit publicado: `1e90437008aafe0a7fde39a4a3a9d6c298a08e93` (merge PR #937).
- Migration: `alembic upgrade head` em `backend/` (já `20260915_0001` head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-DifINXDO.js` / `index-CrdgTMZM.css` (1,800.46 kB / 212.34 kB, gzip 545.69 kB / 33.55 kB). Hash JS novo vs lote 2026-09-12 (`index-CMHlCJ7i.js`) por Favoritos/análise `#935` e velas `#921`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service`. Timer candles: `install-candle-writer-systemd.sh --env prod --force` (timeframes 15m/1h/4h/1d, `#921`); timer já enabled. Primeiro curl público `/api/health` 502 na janela de boot; retry 200. Recidiva #888.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200; `/monitor` serve `index-DifINXDO.js`.

### Evidência de deploy

`1e90437008aafe0a7fde39a4a3a9d6c298a08e93 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker,criptofarol-prod-candle-writer url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-14, com os cards ainda Homologado. Filho isolado `fecho-lote kaizen`. Cards novos `#938` `#939`. Dedupe `#909` `#910` `#658` `#759` `#888` `#868`.

### Drive

Sincronização Google Drive da Clara: ver evidência no closeout (gog).
