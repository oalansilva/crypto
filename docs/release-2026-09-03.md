# Release 2026-09-03

Doc canônica do dia 2026-09-03.

## Lote — cards 782, 784, 786, 790 e 822

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#790` (Produto, P0, Clara) — copy alinhada ao Spot: landing v4, `docs/landing-page.md`, `/help` e chrome do Perfil admitem comprar / stop / vender Spot no Farol (opcional; nunca saque; não é bot 24/7). Cai “nunca envia ordem” / API só leitura como verdade absoluta. `UI impact: affected` (copy). OpenSpec `card-790-copy-spot`. PR #816 `9f2bfe97`.
- `#822` (Operacao, P1, Clara, kaizen) — locators Impeccable cwd-independentes nos quatro clientes (Grok PostToolUse/Stop/PreToolUse/SessionStart, Cursor afterFileEdit/stop, dsh `resolveRepoCwd`, OpenCode directory/worktree). `UI impact: none`. OpenSpec `card-822-impeccable-hook-cwd`. PR #823 `136437d9`.
- `#782` (Operacao, P1, Alan, kaizen) — leftover: quarto adapter DeepSeek dsh (já em `main` via PR #783 `0a0f27e1`). Fecha neste T16.
- `#784` (Operacao, P1, Alan, kaizen) — leftover: dsh always-on #608 (já em `main` via PR #785 `e99999e0`). Fecha neste T16.
- `#786` (Operacao, P1, Alan, kaizen) — leftover: dsh grill no root (já em `main` via PR #796 `aa5dc3b6` / #797 `e71e6402`). Fecha neste T16.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`). `#825` kaizen nascido neste closeout em Em Refinamento.

Responsáveis Clara: `#790` `#822`. Responsáveis Alan: `#782` `#784` `#786`. Copy de landing **alterada** (#790). Pin overlay `v1.1.6`.

### OpenSpec

- Changes arquivadas: `card-790-copy-spot` → `openspec/changes/archive/2026-09-03-card-790-copy-spot/` (`openspec archive -y`, specs synced); `card-822-impeccable-hook-cwd` → `openspec/changes/archive/2026-09-03-card-822-impeccable-hook-cwd/`; `card-784-dsh-608-always-on` → `openspec/changes/archive/2026-09-03-card-784-dsh-608-always-on/`; `card-786-dsh-grill-root` → `openspec/changes/archive/2026-09-03-card-786-dsh-grill-root/`; `card-782-dsh-adapter` → `openspec/changes/archive/2026-09-03-card-782-dsh-adapter/` (`openspec archive -y --skip-specs` após sync agentic: CLI abortou MODIFIED/RENAME do header `Four adapters ship in every consumer` / `Process law has one nucleus and four adapters` ausente no main spec até o rename; specs aplicadas à mão).
- Main specs: `landing-conversion-copy`, `user-onboarding`, `user-preferences-binance-credentials`, `covenant-flow` (quatro adapters + `clients.dsh` extra), `process-harness` (quatro adapters + pele dsh), `process-fsm-guard` (dialeto dsh), `cursor-harness`, `developer-tooling`, `grill-card`, `process-fsm-paging`.
- `openspec validate --all`: 167/167 no `pre`; 162/162 após archive (5 changes saíram).
- Ativas restantes: nenhuma.

### Git e publicação

- `origin/develop` `136437d9` continha só o pacote homologado novo (`#816` `#823`); leftovers `#782` `#784` `#786` já estavam em `origin/main`.
- `release-guard pre` PASS (`RELEASE_CARDS=782,784,786,790,822`; `PRESERVED_BRANCHES` 13 in-flight/leftover; `RELEASE_DATE=2026-09-03`). Homologation comments canônicos dos cinco cards postados via `post-card-evidence-comment.sh --transition homologado` neste turno antes do `pre`.
- PR `develop -> main` #824, merge `5fe8b173369f1414593bcd8bdf5de88ff179f4c3` (`Merge pull request #824 from oalansilva/develop`).
- CI do PR #824: checks `pass` (`backend-tests` 1m50s, `e2e-playwright` 5m16s, `openspec-validate` 18s, `process-fsm` 32s, `frontend-build` 40s, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 824 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-03 -> main` (este commit) com `docs/release-2026-09-03.md` + `docs/kaizen-log.md` + 5 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `5fe8b173369f1414593bcd8bdf5de88ff179f4c3` (merge PR #824).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl, já head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-jZpK-fWS.js` / `index-BSHopo8Y.css` (1,779.20 kB / 212.66 kB, gzip 539.97 kB / 33.58 kB). Hash JS novo vs lote 2026-08-30 (`index-CIK38obo.js`) pela copy #790.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` (`criptofarol-prod-candle-writer.service` é timer, inactive/waiting). Primeiro `GET /api/health` durante o boot devolveu 502; retry ~8s → 200.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200 (copy Spot visível: “permissão Spot Trade (sem saque)”; `/help` 200).

### Evidência de deploy

`5fe8b173369f1414593bcd8bdf5de88ff179f4c3 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-03, com os cards ainda Homologado. Card novo `#825` (Drive/gog sem credencial com copy de produto). Dedupe `#658` `#769` `#759` `#659` `#812`.

### Drive

Sincronização Google Drive da Clara **não executada**: `gog` recusou (`OAuth client credentials missing`, sem `~/.config/gogcli/credentials.json`); `bws secret list` recusou (`Missing access token`). Markdown local `docs/landing-page.md` versionado e a landing pública já serve a copy Spot; Drive fica pendente de credencial nesta sessão. Card kaizen `#825`.
