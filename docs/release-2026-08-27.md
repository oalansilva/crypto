# Release 2026-08-27

Doc canônica do dia 2026-08-27.

## Lote — card 747

Cards `Homologado` incluídos (Alan em chat: configurações DEV também em PROD + `suba a release`):

- `#747` (Monitor, Codex) — alertas Telegram position-aware por usuário (Bot API, DMs individuais, sem Hermes)

Fora do lote: `#749` (P0 UI reload Perfil, Em Refinamento); `#752` kaizen `.env` append-only; worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#728`.

Responsáveis Clara/Alan: nenhum neste lote. Copy de landing inalterada.

### OpenSpec

- Change arquivada: `card-747-position-telegram-dm` → `openspec/changes/archive/2026-08-27-card-747-position-telegram-dm/`.
- Main specs: `openspec/specs/monitor-telegram-alerts/spec.md` (update) e `openspec/specs/user-telegram-alerts/spec.md` (create).
- `openspec archive -y` com sync de specs (sem `--skip-specs`).
- `openspec validate --all`: 158/158 no archive; CI do PR #750 verde.

### Git e publicação

- `origin/develop` continha o pacote homologado (merge PR #748 `bfdd30ab`).
- Branch `release-2026-08-27` = `develop` + archive `6d11c7a8`.
- `release-guard pre`: PASS (`RELEASE_CARDS=747`; `PRESERVED_BRANCHES` in-flight). Comentário Homologado canônico no #747 antes do `pre`.
- PR `release-2026-08-27 -> main` #750, merge `255b8652`.
- CI do PR #750: checks iniciados verdes; `qa-gate` e `deploy-staging` skip (`base_ref != main` / não develop).
- Sync `main → develop`: PR #751 merge `56f957d4`.

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `255b8652` (merge PR #750).
- Migration: `alembic upgrade head` (já head; schema Telegram via `ensure_runtime_schema_migrations` no boot).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-ojMWwVD8.js` / `index-DQ-0-Hba.css`.
- Telegram: `MONITOR_TELEGRAM_*` + `TELEGRAM_WEBHOOK_SECRET` no `.env` PROD; webhook Bot API `https://criptofarol.com.br/api/telegram/webhook`.
- Incidente closeout: `grep` sem permissão + `mv` deixou o `.env` só com 4 linhas Telegram; backend loop (`DATABASE_URL`/`JWT_SECRET`). Restore de `.env.bak-20260809` + `JWT_SECRET` do DEV + chaves Telegram; backup `.env.wiped-20260827`. Residual: JWT compartilhado DEV/PROD neste restore.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok"}`; webhook sem secret → 403 `Invalid webhook secret`.

### Evidência de deploy

`255b8652 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-runtime-worker,criptofarol-prod-discovery-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-27, com o card ainda Homologado. Card kaizen `#752` (append-only `.env`).
