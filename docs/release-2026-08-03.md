# Release 2026-08-03 — Cards 366 e 374

## Pacote

- `#366` — auditoria e otimização da suíte unitária do backend.
- `#374` — integração do Impeccable ao gate de Design do Codex.
- O pacote também carrega o efeito líquido do rollback dos cards cancelados `#362` e `#369`; eles não são candidatos de release e permanecem `Cancelado` no board.

## OpenSpec

- Specs principais sincronizadas: `ci-testing-hardening` e `impeccable-design-gate`.
- Changes arquivadas:
  - `2026-08-03-audit-optimize-backend-unit-tests`
  - `2026-08-03-integrate-impeccable-design-gate`
- `openspec validate --all`: **144/144 válidos**.

## Git e preservação de WIP

- Branch de release: `release-20260803-cards-366-374`.
- Commit de archive/sync: `49b2d5ef`.
- Worktrees auxiliares estavam limpos e foram preservados fora do registro de worktree em `/tmp/preserved-*`.
- Branches remotas preservadas fora do pacote:
  - `origin/card-366-backend-unit-tests`
  - `origin/card-369-qa-evidence`
  - `origin/rollback-card-362-369`
  - `origin/preserve/card-369-behavioral-luna-sandbox-20260803`
- O WIP acima foi classificado como `preservar`, não integrado ao pacote e não descartado.

## Validação local

- Inventário backend: **55/55**, sem entradas stale, ausentes ou duplicadas.
- Contrato do harness: **8 testes aprovados**.
- Suíte unitária backend em PostgreSQL dedicado: **387 aprovados, 4 skips esperados**.
- Banco de teste usado: `crypto_app_test` e `crypto_workflow_test`; bancos DEV/PROD não foram usados.

## Próximas evidências

- Guard `pre`, PR de release para `main`, CI verde e merge manual.
- Restart e validação do endpoint PROD.
- Realinhamento/restart do DEV após a publicação.
- Guard `post` e avanço dos cards `#366` e `#374` para `Pronto`.
