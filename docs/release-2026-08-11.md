# Release 2026-08-11 — Cards 463, 464, 456, 457, 458

Doc canônica do dia 2026-08-11.

## Pacote

Cards `Homologado` incluídos:

- `#463` (P0, Codigo) — Erro ao comprar ativo diz que não tenho saldo em USDT: saldo livre USDT real + dica Simple Earn + saldo no modal. Resp: Codex.
- `#464` (P1, Operacao) — Adicionar coluna Em Refinamento como primeira coluna do board (entrada obrigatória de todo card novo). Resp: Codex.
- `#456` (P1, Operacao) — Kaizen: dedupe de comentários de evidência no fechamento (1 por transição). Resp: Codex.
- `#457` (P1, Operacao) — Kaizen: release-guard post inventaria change-*/card-*/release-* e deleção no closeout. Resp: Codex.
- `#458` (P2, Operacao) — Kaizen: bulk-archive de changes OpenSpec de cards terminais + check no guard. Resp: Codex.

Responsáveis: nenhum card com `Responsável=Clara` ou `Responsável=Alan` → passo documental de produto/Drive N/A para este pacote (mudanças de código/processo/scripts/docs técnicos; Git continua sendo fonte).

## OpenSpec

- Changes ativas no pacote: `fix-saldo-usdt-compra` (#463, permanece ativa — card em fluxo até Pronto), `kaizen-dedupe-card-comments` (#456), `kaizen-guard-branch-inventory` (#457), `kaizen-bulk-archive-terminal-changes` (#458), cada uma com proposal/specs/design/tasks e `Design Agent verdict: PASS` (`UI impact: none` para os três cards kaizen; #463 é código backend/frontend com design aprovado na branch `card-463-saldo-usdt-compra`).
- Bulk archive executado no #458: 33 changes OpenSpec de cards terminais movidas para `openspec/changes/archive/2026-08-11-*/`; 16 capabilities novas sincronizadas; `fix-saldo-usdt-compra` reativada após review (card #463 Homologado em fluxo).
- `openspec validate --all`: **131/131 válidos** (evidência pré-merge).

## Git e publicação

- `develop` continha apenas conteúdo homologado do pacote → PR direto `develop -> main` (#475), merge manual `412ed9ad`.
- Commits do pacote em develop: `fcda1824` (#463, PR #465), `de2dd46e` (#464, PR #466), `b9319ba9` (docs #464, PR #467), `f652532b` (#456, PR #468), `5b0f4813` (#457, PR #471), `47904c82` (#458, PR #473), `2280a391` (docs release, PR #474).
- PRs de integração por card com `qa-gate` verde (openspec, backend-format/unit/integration/coverage, frontend lint/tests/build, e2e-playwright funcional + visual).

## Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `412ed9ad` (`git fetch origin && git reset --hard origin/main`; merge PR #475 `develop -> main`).
- Migration: `alembic upgrade head` → `20260806_0001 (head)` (nenhuma migration nova neste pacote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV="production" npm run build` → built em 10.85s (bundle `index-Ca_PbIWc.js`).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service` (bundle novo servido). `criptofarol-prod-leads.service` e `criptofarol-prod-runtime-worker.service` mantidos ativos (sem mudança).
- Endpoints validados: `https://criptofarol.com.br/` → 200 (bundle `index-Ca_PbIWc.js`); `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites` → 200.

## Conclusão

- Release 2026-08-11 publicada em `main` (merge manual) com deploy PROD validado; cards movidos para `Pronto` após evidência.
