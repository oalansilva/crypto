# Release 2026-08-29

Doc canônica do dia 2026-08-29.

## Lote — cards 787, 792 e 798

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#792` (Produto, P0, frente Risco, Clara) — Card do Monitor: risco explícito (stop, alvo, distância e o que acontece se a leitura falhar) — HOLD com distância/stop/alvo/indisponível + frase cenário condicionada + EXIT sem ressuscitar Entry/Stop + coerência ChartModal + proteção segredo. `UI impact: affected` com protótipo `frontend/public/prototypes/card-792-monitor-risco-explicito/index.html` e snapshots Impeccable r2.
- `#798` (Operacao, P0, Kaizen, sem Responsável → Clara implícita) — Harness: overlay_doc, rules.md e docs cinzentos ainda mandam carregar alan-workflow (path morto pós-pin) — retarget de toda instrução de carga para `covenant-flow` / `covenant-flow-environments`, helper `publish-openspec-card-artifacts.sh`. `UI impact: none`.
- `#787` (Produto, P2, Alan) — README do covenant-flow para quem não conhece o projeto — reescrita PT-BR com bloco do estranho (núcleo vs consumidor vs overlay, canal v1, 4 clientes Auto vs cooperativo, walkthrough 12 colunas), description GitHub `Covenant Flow — processo portátil de 12 colunas (núcleo + adapters)`, tag `v1.1.2` e pin `v1.1.2` no Cripto. `UI impact: none`.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`); branches `card-782-dsh-adapter` / `card-784-dsh-608-always-on` / `card-786-dsh-grill-root` / `card-786-dsh-host-prompt` já mergeadas em `develop` mas `Status=Done` (não Homologado) — publicadas como dependência deste lote via merge `develop->main` (código harness já em `develop`).

Responsáveis Clara/Alan: `#792 Clara`, `#787 Alan`, `#798` sem campo (tratado como Operacao; guard valida via `RELEASE_CARDS`). Copy de landing inalterada. `covenant-flow` produto README atualizado (fora do consumidor, mas pinado).

### OpenSpec

- Changes arquivadas: `card-787-covenant-flow-readme` → `openspec/changes/archive/2026-08-29-card-787-covenant-flow-readme/`; `card-798-overlay-doc-covenant-flow` → `openspec/changes/archive/2026-08-29-card-798-overlay-doc-covenant-flow/`; `card-792-monitor-risco-explicito` → `openspec/changes/archive/2026-08-29-card-792-monitor-risco-explicito/` (commit docs-release, `openspec archive -y` com sync de specs).
- Main specs: `openspec/specs/covenant-flow/spec.md` (+5, tag v1.1.2), `openspec/specs/consumer-load-docs/spec.md` (create +3, retarget alan-workflow→covenant-flow), `openspec/specs/opportunity-monitor/spec.md` (+5, risco explícito HOLD/EXIT/indisponível).
- `openspec validate --all`: 164/164 no docs-release branch; 166/166 antes do archive (pre), 164 após (specs atualizadas).

### Git e publicação

- `origin/develop` continha o pacote homologado + dependências harness já mergeadas (HEAD `80166268`).
- `release-guard pre`: PASS (`RELEASE_CARDS=787,792,798`; `RELEASE_BRANCHES=card-787-covenant-flow-readme,card-792-monitor-risco-explicito,card-798-overlay-doc-covenant-flow`; `PRESERVED_BRANCHES` 12 in-flight; `RELEASE_DATE=2026-08-29`). Homologation comments canônicos postados via `post-card-evidence-comment.sh --transition homologado` antes do `pre` (`5e2db7fb`/`b4b0cf6c`/`80166268`).
- PR `develop -> main` #804, merge `3791b6f7` (`Merge pull request #804 from oalansilva/develop`).
- CI do PR #804: 13 checks `pass` (`backend-tests`, `frontend-tests`, `e2e-playwright` 5m10s, `openspec-validate`, `process-fsm`, `changes`, `frontend-build`, `backend-format`, `qa-policy`, `new-route-playwright-coverage`) + `qa-gate`/`deploy-staging`/`backend-coverage-gate` `skipping` (base `main` esperado para release). Watcher `gh pr checks --watch --fail-fast --interval 20` verde.
- Docs PR: `docs-release-2026-08-29 -> main` (este commit) com `docs/release-2026-08-29.md` + `docs/kaizen-log.md` + 3 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final; reexecutar `post` se diverge).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `3791b6f7` (merge PR #804).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl, já head `20260823_0001`; sem migration nova para 787/798/792).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CIK38obo.js` / `index-BSHopo8Y.css` (1,779.03 kB / 212.66 kB, gzip 539.89 kB / 33.58 kB).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` ( `criptofarol-prod-candle-writer.service` é timer, execução pontual 0/SUCCESS).
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200 (Caddy, `content-length 26619`).

### Evidência de deploy

`3791b6f7 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-29, com os cards ainda Homologado. Achados de homologation timing (corrigido neste lote), tasks.md pendente #792 e dirty worktrees já cobertos por #658/#659/#759. Sem card kaizen novo; dedupe auditável.
