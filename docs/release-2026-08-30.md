# Release 2026-08-30

Doc canônica do dia 2026-08-30.

## Lote — cards 787, 795, 799, 801 e 809

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#795` (Operacao, P0, Alan) — Processo: quatro clientes cooperativos; Cursor deixa de ser exceção Auto. Overlay `clients.cursor.auto: false`; stub always-on sem exceção Auto. `UI impact: none`. OpenSpec `card-795-quatro-clientes-cooperativos`. PR #807 `98ef22ab`.
- `#799` (Operacao, P0, Alan, kaizen) — protótipo de tela existente MUST clonar a página /rota, não galeria de estados (recidiva #792). T5/`G_design` recusa proto que não clona. `UI impact: none`. OpenSpec `card-799-prototype-clone-rota`. PR #806 `d9ff31ed`.
- `#801` (Operacao, P0, Alan, kaizen) — QA verde não fecha Done: T11 exige PR (`no_pr`); T14 classifica `qa-gate` e devolve causa visível (`qa-gate pending` / `qa-gate failed` / `sync: dirty` / `I8`). `UI impact: none`. OpenSpec `card-801-t14-qa-closeout`. PR #808 `be8776c4`.
- `#809` (Operacao, P0, Alan, kaizen) — grill-card para no DoD de operador; árvore de desenho só no Design. Pin produto `v1.1.6`. `UI impact: none`. OpenSpec `card-809-grill-card-operator-ceiling`. PR #810 `8a088d0c`.
- `#787` (Produto, P2, Alan) — leftover do lote 2026-08-29 (README covenant-flow; já em `main` via PR #804 `3791b6f7`). Status ainda Homologado; fecha neste T16.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`); `#782` / `#784` / `#786` em `Done` (não Homologado); `#812` kaizen nascido neste closeout em Em Refinamento.

Responsáveis Alan: `#795` `#799` `#801` `#809` `#787`. Copy de landing inalterada. Pin overlay `v1.1.6`.

### OpenSpec

- Changes arquivadas: `card-795-quatro-clientes-cooperativos` → `openspec/changes/archive/2026-08-30-card-795-quatro-clientes-cooperativos/`; `card-799-prototype-clone-rota` → `openspec/changes/archive/2026-08-30-card-799-prototype-clone-rota/`; `card-801-t14-qa-closeout` → `openspec/changes/archive/2026-08-30-card-801-t14-qa-closeout/` (`openspec archive -y --skip-specs` após sync agentic: CLI abortou MODIFIED do header `T10-T13 events imply exclusive-group guards` ausente no main spec até o rename; specs aplicadas à mão); `card-809-grill-card-operator-ceiling` → `openspec/changes/archive/2026-08-30-card-809-grill-card-operator-ceiling/`.
- Main specs: `covenant-flow` (quatro clientes cooperativos + tecto grill + QA closeout client-labeled), `process-harness`, `process-fsm-event` (T11 `no_pr` + T14 classificado), `process-fsm`, `process-fsm-guard` (`canonical_card_branch`), `cursor-harness`, `grill-card`, `impeccable-design-gate`, `llm-flow-emission`; spec nova `design-route-clone-gate`.
- `openspec validate --all`: 168/168 no `pre`; 165/165 após archive (4 changes saíram; spec nova `design-route-clone-gate`).
- Ativas restantes (fora do pacote, cards `Done`): `card-782-dsh-adapter`, `card-784-dsh-608-always-on`, `card-786-dsh-grill-root`.

### Git e publicação

- `origin/develop` `8a088d0c` continha só o pacote homologado novo (`#807` `#806` `#808` `#810`); leftover `#787` já estava em `origin/main`.
- `release-guard pre` PASS (`RELEASE_CARDS=787,795,799,801,809`; `PRESERVED_BRANCHES` 16 in-flight/Done; `RELEASE_DATE=2026-08-30`). Homologation comments canônicos de `#795` `#799` `#801` `#809` postados via `post-card-evidence-comment.sh --transition homologado` neste turno antes do `pre` (`98ef22ab` / `d9ff31ed` / `be8776c4` / `8a088d0c`). `#787` já tinha comentário do lote anterior.
- PR `develop -> main` #811, merge `ee4a3e3ffe3f875b1c4ff0feae8f1ecc2c58d12c` (`Merge pull request #811 from oalansilva/develop`).
- CI do PR #811: checks `pass` (`backend-tests` 2m2s, `e2e-playwright` 5m12s, `openspec-validate` 14s, `process-fsm` 32s, `frontend-build` 36s, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 811 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-08-30 -> main` (este commit) com `docs/release-2026-08-30.md` + `docs/kaizen-log.md` + 4 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `ee4a3e3ffe3f875b1c4ff0feae8f1ecc2c58d12c` (merge PR #811).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl, já head; sem migration nova neste lote harness).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CIK38obo.js` / `index-BSHopo8Y.css` (1,779.03 kB / 212.66 kB, gzip 539.89 kB / 33.58 kB) — hashes iguais ao lote 2026-08-29 (zero diff `frontend/src`).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` (`criptofarol-prod-candle-writer.service` é timer, inactive/waiting).
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200 (Caddy, `content-length 26619`).

### Evidência de deploy

`ee4a3e3ffe3f875b1c4ff0feae8f1ecc2c58d12c services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-30, com os cards ainda Homologado. Card novo `#812` (T16 leftover). Dedupe `#658` `#769` `#759` `#659`.

### Drive

Sincronização Google Drive da Clara **não executada**: `gog` recusou (`OAuth client credentials missing`, sem `~/.config/gogcli/credentials.json`); `bws secret list` recusou (`Missing access token`). Markdown local versionado; Drive fica pendente de credencial nesta sessão. Lote harness (sem copy de landing).
