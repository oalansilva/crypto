# Release 2026-08-09 — Cards 385, 413, 416, 420 e Kaizen 421, 422, 423, 428, 430, 438, 439, 440

Doc canônica do dia 2026-08-09 (consolidada a partir de `release-2026-08-09-cards-385-413-416.md` e `release-2026-08-09.md` — F-1 da auditoria kaizen; uma doc por data).

## Release 1 — Cards 385, 413, 416 (PR #437, merge `27f9e1bf`)

### Pacote

Cards `Homologado` incluídos:

- `#385` — Monitor: comprar e vender ativo Spot direto pela Binance (venda a market). Resp: Codex. P1.
- `#413` — Carteira mais fácil leitura (zebra na tabela e remoção de textos redundantes). Resp: Codex.
- `#416` — Substituir gpt-5.6-luna por Qwen3.7 Plus no fluxo de análise visual (vision-router). Resp: opencode. P2.

Responsáveis: nenhum card com `Responsável=Clara` ou `Responsável=Alan` → passo documental de produto/Drive N/A para este pacote (sem docs de produto alterados).

### OpenSpec

- Changes ativas no pacote: `card-385-monitor-direct-spot-trading`, `wallet-easier-reading`, `card-416-substituir-luna-por-qwen3`.
- `openspec validate --all`: **150/150 válidos** (evidência pré-merge).
- Pós-archive: 3 changes arquivadas; 4 delta specs sincronizadas; `openspec validate --all` 148/148.

### Git e publicação

- PR `develop -> main` (#437), merge manual `27f9e1bf`.
- Commits do pacote: `95e7f7d4` (#385), `288c17ad` (docs #385), `985d1697` (#413), `88a2a59d` (docs #413), `c0045264` (#416), `59526270` (guard post docs).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `27f9e1bf`.
- Migration: `alembic upgrade head` → `20260803_0001 -> 20260806_0001` (Monitor direct Spot — #385).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV="production" npm run build` → built em 9.89s.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`; leads e runtime-worker mantidos ativos.
- Endpoints validados: `https://criptofarol.com.br/api/health` → `{"status":"ok"}`; `https://criptofarol.com.br/` → 200.

## Release 2 — Card 420 (Agente Kaizen, PR `release-20260809-card420`)

### Pacote

- `#420` — Kaizen: criar agente de melhoria contínua de processo (auditoria, sessões opencode e registro PO). `Homologado` por Alan.

### OpenSpec

- Change arquivada em `openspec/changes/archive/2026-08-09-card-420-kaizen-agent/`.
- Delta specs sincronizadas: `kaizen-continuous-improvement`, `developer-tooling`, `multiagent-operating-standard`.
- `openspec validate --all`: **147/147** após archive.

### Git e publicação

- Branch de release: `release-20260809-card420` (a partir de `main`; develop continha conteúdo não homologado).
- Commits: `78a572f6` (feat #420), `7409d419` (archive OpenSpec).
- Correções pós-publicação em main via PR #433 (`6b8cca24`); develop realinhada via PR #434 (`6c062ce8`).

### Deploy PROD

- Commit publicado: `e2c89d4d`.
- Migration: `20260803_0001 (head)` (nenhuma migration nova).
- Frontend: build executado para evidência de coerência (pacote tooling/docs).
- Services PROD: todos `active` (nenhum runtime alterado).
- Endpoints validados: `http://127.0.0.1:8003/api/health` e `https://criptofarol.com.br/api/health` → ok; `/` → 200.

## Release 3 — Cards Kaizen 421, 422, 423, 428, 430, 438, 439, 440 (PR #451, merge `4e7c125f`)

### Pacote

Cards `Homologado` incluídos (auditorias kaizen 2026-08-08 e 2026-08-09):

- `#421` (P0) — Evidência de aprovação de Design antes de aplicar código; BLOCKED exige resolução registrada.
- `#422` (P1) — release-guard exige deploy PROD antes de Pronto e inventaria refs órfãs no post.
- `#423` (P1, elevado de P2) — helper publish-openspec-card-artifacts atualiza Gist existente; retrigger CI sem commit vazio.
- `#428` (P1) — release-guard post detecta change OpenSpec duplicada (ativa + arquivada).
- `#430` (P2) — sync título board/issue no fechamento; sessão nova após troca de modelo de subagent.
- `#438` (P1) — release-guard post valida evidência documental e campos do board antes de Pronto.
- `#439` (P2) — path-check antes de delegar análise visual (zero respawn por arquivo inexistente).
- `#440` (P2) — fechamento exige todos completed + título de sessão informativo; elevar #423 a P1.

Responsáveis: nenhum card com `Responsável=Clara` ou `Responsável=Alan` → passo documental de produto/Drive N/A (mudanças de processo/scripts/docs técnicos; Git continua sendo fonte).

### OpenSpec

- 8 changes ativas no pacote (`card-421-*` … `card-440-*`), cada uma com proposal/specs/design/tasks e `Design Agent verdict: PASS` (`UI impact: none`).
- `openspec validate --all`: **156/156 válidos** (evidência pré-merge).

### Git e publicação

- `develop` continha apenas conteúdo homologado do pacote → PR direto `develop -> main` (#451), merge manual `4e7c125f`.
- PRs de integração por card (#443-#450) com `qa-gate` verde (openspec, backend-format/unit/integration/coverage, frontend lint/tests/build, e2e-playwright funcional + visual).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (git safe.directory via sudo; `.git` owned by root).
- Commit publicado: `4e7c125f` (`git fetch origin && git reset --hard origin/main`; merge PR #451 `develop -> main`).
- Migration: `alembic upgrade head` → `20260806_0001 (head)` (nenhuma migration nova neste pacote).
- Frontend: `VITE_APP_ENV=production npm --prefix frontend run build` → built em 10.21s (pacote não altera frontend; build executado para evidência de coerência).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service` (bundle novo servido). `criptofarol-prod-leads.service` e `criptofarol-prod-runtime-worker.service` mantidos ativos (sem mudança).
- Endpoints validados: `https://criptofarol.com.br/` → 200 (bundle `index-lsLhW6b5.js`); `/api/health` → 200; `/login`, `/app`, `/dashboard`, `/monitor` → 200.

## Auditoria kaizen pós-release

- Executada `/kaizen release` após deploy PROD validado e antes de mover cards para `Pronto` (regra 14).
- Relatório completo em `docs/kaizen-log.md`.

## Conclusão

- Três releases do dia 2026-08-09 publicadas em `main` (merges `27f9e1bf`, `e2c89d4d`/`6b8cca24`, `4e7c125f`) com deploy PROD validado; cards movidos para `Pronto` após evidência.
