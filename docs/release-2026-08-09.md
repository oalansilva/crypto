# Release 2026-08-09 — Card 420 (Agente Kaizen)

## Pacote

- `#420` — Kaizen: criar agente de melhoria contínua de processo (auditoria, sessões opencode e registro PO). `Homologado` por Alan.

## OpenSpec

- Change arquivada em `openspec/changes/archive/2026-08-09-card-420-kaizen-agent/`.
- Delta specs sincronizadas para as specs principais:
  - `kaizen-continuous-improvement` (nova: auditoria read-only, sessões escopo release, cards PO, priorização, skills evolution, segurança de output)
  - `developer-tooling` (adicionada: `Subagent kaizen e command disponíveis`)
  - `multiagent-operating-standard` (adicionada: `O papel Kaizen integra o modelo operacional`)
- `openspec validate --all`: **147/147 válidos** após archive.

## Git e publicação

- Branch de release: `release-20260809-card420` (criada a partir de `main` porque `develop` contém conteúdo não homologado: #385, #413, #416 em `Done`).
- Commits no pacote:
  - `78a572f6` — feat: concluir card #420 (cherry-pick do squash aprovado)
  - `7409d419` — release: arquivar OpenSpec e sync specs do card 420
- PR `release-20260809-card420 -> main`: merge manual (a preencher após merge).

## Classificação do release-guard pre (blockers)

Todos os 13 blockers são WIP de cards fora do pacote, classificados como **preservar** (proibido tocar sem autorização — não entram nesta release):

| Item | Card | Classificação |
| --- | --- | --- |
| worktree `crypto-worktrees/card-416-substituir-qwen3` | #416 (Done) | preservar até homologação |
| worktree `/tmp/criptofarol-card-385` + `change-385-monitor-direct-spot-trading` | #385 (Done) | preservar até homologação |
| worktree `/tmp/criptofarol-card-385-close` + `change-385-close` | #385 | preservar (branch close) |
| worktree `/tmp/criptofarol-card-413` + `change-413-wallet-easier-reading` | #413 (Done) | preservar até homologação |
| worktree `/tmp/criptofarol-card-413-close` + `change-413-close` | #413 | preservar (branch close) |
| worktree `/tmp/criptofarol-card-413-preserve` (dirty, WIP evidência) + `preserve/change-413-wallet-zebra-20260808` | #413 | preservar (WIP de evidência) |
| branch local `card-420-kaizen-agent` | #420 | limpar após merge (conteúdo já no pacote) |
| branch local `card-416-substituir-qwen3` | #416 | preservar até homologação |

Nenhum blocker pertence ao conteúdo do pacote #420.

## Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (git safe.directory via sudo; `.git` owned by root).
- Commit publicado: `e2c89d4d` (`git fetch origin && git reset --hard origin/main`).
- Migration: `alembic upgrade head` = `20260803_0001 (head)` (nenhuma migration nova neste pacote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV="production" npm run build` → built em 10.86s (pacote não altera frontend; build executado para evidência de coerência).
- Services PROD: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-leads.service` — todos `active` (nenhum runtime alterado por este pacote tooling/docs; restart não necessário).
- Endpoints validados: `http://127.0.0.1:8003/api/health` e `https://criptofarol.com.br/api/health` → `{"status":"ok"}`; `https://criptofarol.com.br/` → 200.

## Preservação de WIP

- Cards #385, #413, #416 permanecem em `Done` no board (não homologados) com branches/worktrees preservadas.

## Auditoria kaizen pós-release

- Executada `/kaizen release` após deploy PROD validado e antes de mover para `Pronto` (regra 14).
- Relatório completo em `docs/kaizen-log.md` (achados F-1..F-6).
- F-1 corrigido no fechamento (PR #427); cards kaizen #428 (P1), #429 (P1), #430 (P2) criados em `Status=Todo`.
