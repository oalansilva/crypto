# Release 2026-08-26

Doc canônica do dia 2026-08-26.

## Lote — cards 686 + 687

Cards `Homologado` incluídos (Alan em chat: `esta homologado suba a releas…`):

- `#686` (P0, Seguranca, Codex) — proteger `GET /api/logs/tail` (auth)
- `#687` (P0, Operacao/Seguranca, Codex) — remover `.env.binance` do tip + `.gitignore` `.env.*` com allowlist; **sem** rotação de chaves Binance (Alan T6: repo privado + APIs sem withdraw)

Fora do lote: worktrees in-flight `#600` / `#614` / `#604` / `#728` / leftovers `#472` / `#606`; kaizen `#658` / `#659` / `#660` Em Refinamento.

Responsáveis Clara/Alan: nenhum neste lote. Copy de landing inalterada.

### OpenSpec

- Changes arquivadas em 2026-08-26: `card-686-logs-tail-auth` → `openspec/changes/archive/2026-08-26-card-686-logs-tail-auth/`; `card-687-remove-env-binance` → `openspec/changes/archive/2026-08-26-card-687-remove-env-binance/`.
- Main specs sincronizadas: `openspec/specs/log-viewer/spec.md`, `openspec/specs/logging/spec.md`, `openspec/specs/env-binance-git-hygiene/spec.md`.
- `openspec archive -y` com sync de specs (sem `--skip-specs`).
- `openspec validate --all`: verde no CI do PR #738 / #739.

### Git e publicação

- `origin/develop` continha o pacote homologado (HEAD pré-release `dca69739`).
- Branch `release-2026-08-26` = `dca69739` + archive `95be91bd`.
- `release-guard pre`: PASS (`RELEASE_CARDS=686,687`; `PRESERVED_BRANCHES` in-flight).
- Comentários Homologado canônicos e `Responsável=Codex` preenchidos neste turno (antes/durante o `pre`).
- PR `release-2026-08-26 -> main` #738, merge squash `ef053514`.
- CI do PR #738: checks iniciados verdes; `e2e-playwright` pass; `qa-gate` e `deploy-staging` skip (`base_ref != develop`).
- Sync `main → develop`: PR #739 merge `507a5422`.

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `ef053514` (pacote; merge PR #738).
- Migration: `alembic upgrade head` (já head; sem migration nova).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-Ch61YtiE.js` / `index-DzSLxG6d.css`.
- Closeout `#687`: `BINANCE_API_KEY` / `BINANCE_API_SECRET` **já presentes** no `.env` raiz de PROD (presença só; sem gerar/revogar chaves). Tip de `main` sem `.env.binance`; `.gitignore` ignora `.env.*` com allowlist. Valor não entrou em git, unit `Environment=`, chat, issue ou evidência.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`. Interno `127.0.0.1:8003/api/health` → 200.

### Evidência de deploy

`ef053514 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-runtime-worker,criptofarol-prod-discovery-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-26, com os cards ainda Homologado. Sem card kaizen novo; F-1 coberto por `#658` (Em Refinamento).
