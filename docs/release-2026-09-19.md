# Release 2026-09-19

Doc canônica do dia 2026-09-19.

## Lote — cards 967, 968, 969, 970, 975

Cards `Homologado` incluídos (Alan: `suba a release`):

- `#967` (Produto, P1, bug) — Descoberta reconstitui Acompanhar da varredura ativa em PROD. `UI impact: affected`. OpenSpec `card-967-discovery-recupera-varredura-ativa`. PR #971.
- `#968` (Operacao, P1, Alan) — Publish PROD reinicia todos os units de longa duração (incl. discovery-worker); `release-guard` valida `services=`. `UI impact: none`. OpenSpec `card-968-prod-restart-long-running`. PR #972.
- `#969` (Produto, P1) — Selo GO/NO-GO da Descoberta sem piso Combo de 100 trades. `UI impact: affected`. OpenSpec `card-969-discovery-selo-go-nogo`. PR #974.
- `#970` (Produto, P0, bug) — Favoritos deixa de ficar em Carregando / fingir catálogo vazio. `UI impact: affected`. OpenSpec `card-970-favoritos-carga-nao-vazio`. PR #973.
- `#975` (Produto, P0, bug) — Monitor não finge catálogo vazio com Favoritos carregados. `UI impact: affected`. OpenSpec `card-975-monitor-vazio-favoritos`. PR #976.

Caminho `release-*` (push do archive OpenSpec em `develop` recusado por `qa-gate`). Worktrees in-flight preservadas via `PRESERVED_BRANCHES` (#720, #897, #969/#970 locais, branches do pacote, `wip-board-prototype`). Kaizen: ver `docs/kaizen-log.md` (2026-09-19).

Pin overlay `v1.1.16`.

### OpenSpec

- Changes arquivadas: `2026-09-19-card-967-discovery-recupera-varredura-ativa` … `2026-09-19-card-975-monitor-vazio-favoritos` (5 dirs em `openspec/changes/archive/`).
- Main specs novas/atualizadas: `favorites-load-states`, `monitor-first-load`, deltas em discovery/favorites/monitor/opportunity-monitor/release-worktree-hygiene/prod-discovery-workers.
- `openspec validate --all`: 173/173 na branch `release-2026-09-19`.

### Git e publicação

- `origin/develop` `bf55bf2a` (código homologado) + archive local `f382861e` em `release-2026-09-19`.
- `release-guard pre` PASS (`RELEASE_DATE=2026-09-19`, `RELEASE_CARDS=967,968,969,970,975`).
- Comentários Homologado canónicos postados (`bf55bf2a`).
- PR `release-2026-09-19 → main` #977, merge `65c8ffdc007084de781672e35cd6520e3268e4a6`.
- CI PR #977: checks verdes (`e2e-playwright` ~7m14s; `backend-unit-tests` ~7m58s; `qa-gate` skipping em base `main`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (sudo).
- Commit publicado: `65c8ffdc007084de781672e35cd6520e3268e4a6`.
- Migration: `alembic upgrade head` (sem migration nova).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → `index-CGPfcjC3.js` / `index-DHIFMOoi.css`.
- Services reiniciados (janela publish = `services` − `oneshot_services`): `criptofarol-prod-backend`, `criptofarol-prod-frontend`, `criptofarol-prod-leads`, `criptofarol-prod-runtime-worker`, `criptofarol-prod-discovery-worker`.
- Oneshot (fora da janela): `criptofarol-prod-candle-writer`, `criptofarol-prod-telegram-alert-scan` — job/timer à espera.
- Endpoints: `https://criptofarol.com.br/api/health` 200; `/` 200 com bundle `index-CGPfcjC3.js`.

### Evidência de deploy

`65c8ffdc007084de781672e35cd6520e3268e4a6 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker,criptofarol-prod-discovery-worker url=https://criptofarol.com.br`

### Kaizen

Entrada em `docs/kaizen-log.md` (2026-09-19). Filho `fecho-lote kaizen`. Cards kaizen materializados no closeout documental (ver log).

### Drive

Sincronização Google Drive: pendente neste turno (gog / `GOG_KEYRING_PASSWORD`).
