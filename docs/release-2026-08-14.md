# Release 2026-08-14 — Cards 470, 480, 481, 482, 489, 491

Doc canônica do dia 2026-08-14.

## Pacote

Cards `Homologado` incluídos:

- `#470` (P1, Codigo) — Backtest: validação walk-forward (out-of-sample) como gate de promoção a favorito. Resp: Codex.
- `#480` (P1, Operacao) — Kaizen: guard exige evidência de homologação no closeout + post retroativo. Resp: Codex.
- `#481` (P1, Operacao) — Kaizen: triagem de cards presos por coluna + alerta de idade no guard. Resp: Codex.
- `#482` (P2, Operacao) — Kaizen: divergência board/issue em rename pós-Done exige nota + warn no guard. Resp: Codex.
- `#489` (P1, Codigo) — Excluir templates de teste Quant da tela de seleção de combos (/combo/select). Resp: Codex.
- `#491` (P1, Operacao) — design-planner GPT Sol High: modelo frontier no gate Design (specs, crítica e protótipo). Resp: Codex.

Responsáveis: nenhum card com `Responsável=Clara` ou `Responsável=Alan` → passo documental de produto/Drive N/A para este pacote (mudanças de código/processo/scripts/docs técnicos; Git continua sendo fonte).

## OpenSpec

- Changes ativas no pacote: `walk-forward-gate` (#470), `kaizen-homologacao-evidence-comment` (#480), `kaizen-stuck-cards-age-alert` (#481), `kaizen-board-issue-rename-note` (#482), `delete-quant-templates` (#489), `design-planner-grok-4-6` (#491), cada uma com proposal/specs/design/tasks e gate de Design aprovado.
- `openspec validate --all`: **137/137 válidos** (evidência pré-merge).

## Git e publicação

- `develop` continha apenas conteúdo homologado do pacote → PR direto `develop -> main`, merge manual.
- PRs de integração por card com `qa-gate` verde (openspec, backend-format/unit/integration/coverage, frontend lint/tests/build, e2e-playwright funcional + visual).

## Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `05887330` (`git fetch origin && git reset --hard origin/main`; merge PR #506 `develop -> main`).
- Migration: `alembic upgrade head` → `20260806_0001 (head)` (nenhuma migration nova neste pacote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV="production" npm run build` → built em 10.37s (bundle `index-BhUUJd5y.js`).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service` (bundle novo servido). `criptofarol-prod-leads.service` e `criptofarol-prod-runtime-worker.service` mantidos ativos (sem mudança).
- Endpoints validados: `https://criptofarol.com.br/` → 200 (bundle `index-BhUUJd5y.js`); `/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites` → 200.

## Conclusão

- Release 2026-08-14 publicada em `main` (merge manual) com deploy PROD validado; cards movidos para `Pronto` após evidência.
