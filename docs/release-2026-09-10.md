# Release 2026-09-10

Doc canônica do dia 2026-09-10.

## Lote — cards 718, 861, 872, 876, 879, 880, 884

Cards `Homologado` incluídos (Alan em chat: `suba a relese`):

- `#718` (Produto, P2, Clara, Monitor) — um nome canónico por estado da board (`Em posição` / `Saída / cobertura`); hint `⌘K` removido da busca; `Compra`/`Venda` só no lado da ordem. `UI impact: affected`. OpenSpec `card-718-monitor-naming-atalho`. PR #877 `04677767`. Sem residual `[ ]`.
- `#861` (Produto, P0, Alan, Monitor) — `Remover stop` com confirmação no gráfico e no fluxo de venda travada pela stop (não dispara a venda). Follow-up #875 `06eb0e1e` no erro real. `UI impact: affected`. OpenSpec `card-861-remover-stop`. PR #874 `7580e7ba`. Residual 3.2 (witness BTC Spot HOLD) permanece `[ ]`.
- `#872` (Operacao, P0, Alan) — dsh via túnel cola links GitHub clicáveis; proibido `xdg-open` de docs de release. `UI impact: none`. OpenSpec `card-872-dsh-closeout-links`. PR #873 `d1e1dda2`. Sem residual `[ ]`.
- `#876` (Produto, P0, Clara, Backtest) — Descoberta corta o grid quando a listagem não chega a 30 negócios; quarto saco `amostra insuficiente` (selo distinto de `Baixa amostra`). `UI impact: affected`. OpenSpec `card-876-discovery-amostra-insuficiente`. PR #878 `fd601691`. Sem residual `[ ]`.
- `#879` (Operacao, P1, Alan) — destapar o pai após filho `completed`; no review, colar o diff materializado. `UI impact: none`. OpenSpec `card-879-destapar-pai-colar-diff`. PR #883 `2dc937e3`. Sem residual `[ ]`.
- `#880` (Operacao, P1, Alan) — dois modos Cursor na mesma VM (terminal + Desktop+SSH) sem partir o host. `UI impact: none`. OpenSpec `card-880-fluxo-cursor-vm-desktop-ssh`. PR #881 `0176a3d5`. Residual 5.3 (prova viva Windows+SSH) permanece `[ ]`.
- `#884` (Operacao, P1, Alan) — um Apply por coluna e review em onda no mesmo turno (sem pingue-pongue). `UI impact: none`. OpenSpec `card-884-apply-por-coluna-review-onda`. PR #885 `8f988d4d`. Sem residual `[ ]`.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` (preservadas via `PRESERVED_BRANCHES`); leftover `#718` dirty `.impeccable/critique` classificado; `preserve/board-prototype-wip` (stashes de board-prototype alheio classificados neste turno); branch remota órfã `origin/card-469-idempotency-normalization-wip` (WIP preservado fora do lote desde 2026-08-17). `#888` kaizen nascido neste closeout.

Responsáveis Clara: `#718` `#876`. Responsáveis Alan: `#861` `#872` `#879` `#880` `#884`. Copy de landing **inalterada** (V4 `Compra`/`Venda` permanece decisão prévia). Pin overlay `v1.1.14`.

### OpenSpec

- Changes arquivadas: `card-872-dsh-closeout-links` → `openspec/changes/archive/2026-09-10-card-872-dsh-closeout-links/` (spec nova `closeout-links`); `card-861-remover-stop` → `openspec/changes/archive/2026-09-10-card-861-remover-stop/`; `card-718-monitor-naming-atalho` → `openspec/changes/archive/2026-09-10-card-718-monitor-naming-atalho/`; `card-876-discovery-amostra-insuficiente` → `openspec/changes/archive/2026-09-10-card-876-discovery-amostra-insuficiente/`; `card-880-fluxo-cursor-vm-desktop-ssh` → `openspec/changes/archive/2026-09-10-card-880-fluxo-cursor-vm-desktop-ssh/` (spec nova `cursor-host-modes`); `card-879-destapar-pai-colar-diff` → `openspec/changes/archive/2026-09-10-card-879-destapar-pai-colar-diff/`; `card-884-apply-por-coluna-review-onda` → `openspec/changes/archive/2026-09-10-card-884-apply-por-coluna-review-onda/` (`openspec archive -y`; `#884` com `--skip-specs`: headers ADDED já existiam no main spec pós-merge + archive `#879`; contrato same-turn vive nas requirements dedicadas).
- Main specs atualizadas: `closeout-links` (nova), `monitor-direct-spot-trading`, `monitor-spot-stop-limit`, `monitor`, `discovery-leaderboard`, `discovery-sweep`, `discovery-three-modes`, `cursor-harness`, `cursor-host-modes` (nova), `llm-flow-emission`, `cursor-code-review`.
- `openspec validate --all`: 170/170 no `pre` (7 changes ativas); 165/165 após archive.
- Ativas restantes: nenhuma.

### Git e publicação

- `origin/develop` `8f988d4d` continha só o pacote homologado (`#873` `#874` `#875` `#877` `#878` `#881` `#883` `#885`).
- `release-guard pre` PASS (`RELEASE_CARDS=718,861,872,876,879,880,884`; `PRESERVED_BRANCHES` in-flight + leftover `#718` + `preserve/board-prototype-wip`; `RELEASE_DATE=2026-09-10`). Comentários Homologado canónicos dos sete cards postados neste turno antes do `pre`. Campos Responsável/Prioridade/Tipo preenchidos neste turno (`#718` Clara; `#879`/`#880`/`#884` Alan/P1/Operacao).
- PR `develop -> main` #887, merge `e219d6dd453eeb0237ff6b2e8a687755903239b2` (`Merge pull request #887 from oalansilva/develop`).
- CI do PR #887: checks `pass` (`backend-tests` 2m16s, `backend-unit-tests` 4m30s, `e2e-playwright` 5m9s, `openspec-validate` 17s, `process-fsm` 44s, `frontend-build` 45s, `frontend-lint`, `frontend-tests`, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`, `changes`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 887 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-10 -> main` (este commit) com `docs/release-2026-09-10.md` + `docs/kaizen-log.md` + `docs/mvp-scope.md` + `docs/decision-log.md` + 7 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (root-owned; operações com `sudo`).
- Commit publicado: `e219d6dd453eeb0237ff6b2e8a687755903239b2` (merge PR #887).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl; `20260823_0001 → 20260909_0001` discovery `insufficient_sample`).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index--3lXkM0I.js` / `index-Cg5eQOv5.css` (1,786.04 kB / 211.56 kB, gzip 542.02 kB / 33.42 kB). Hash JS novo vs lote 2026-09-08 (`index-BV0bshgg.js`) por Monitor #718/#861 e Descoberta #876.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` (`criptofarol-prod-candle-writer.service` timer, inactive). Primeiro curl público `/api/health` 502 na janela de boot (~2s; uvicorn ainda em startup); retry 200.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200; `/monitor` serve `index--3lXkM0I.js`.

### Evidência de deploy

`e219d6dd453eeb0237ff6b2e8a687755903239b2 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-10 (subsecção lote 718/861/872/876/879/880/884), com os cards ainda Homologado. Card novo `#888` (health PROD 502 na janela de boot). Dedupe `#658` `#769` `#759` `#840` `#825` `#868`.

### Drive

Sincronização Google Drive da Clara **não executada**: `gog` `OAuth client credentials missing`; `bws` sem access token. Markdown local atualizado (`mvp-scope` item 4, `decision-log` #718/#861/#876). Recidiva `#825`.
