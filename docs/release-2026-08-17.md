# Release 2026-08-17 — Cards 469, 502, 503, 504, 516, 517, 518, 562

Doc canônica do dia 2026-08-17.

## Pacote

Cards `Homologado` incluídos:

- `#469` (P1, Codigo) — Backtest: descoberta sistemática de estratégias swing com leaderboard. Resp: Codex.
- `#502` (P1, Produto) — Combo: Ver logs inicia vazio e faz autoscroll. Resp: Codex.
- `#503` (P1, Produto) — Reavaliar holdout OOS com Treino (IS) separado. Resp: Codex.
- `#504` (P1, Codigo) — Digest canônico da prova OOS no round-trip JSON. Resp: Alan.
- `#516` (P0, Operacao) — Kaizen: completar #509 (GraphQL fail-closed e higiene de closeout). Resp: Codex.
- `#517` (P1, Operacao) — Kaizen: release-guard flagra OpenSpec terminal sem id. Resp: Codex.
- `#518` (P1, Operacao) — Kaizen: fechamento de release como gate. Resp: Codex.
- `#562` (P0, Codigo) — Migrar harness operacional para Cursor Agent (cutover OpenCode). Resp: Codex.

Fora do pacote: `#550` e `#559` Cancelados (substituídos pelo #562). `#549` isolado em `card-549-unify-strategy-title-description` (Em Refinamento). WIP pós-homologação do #469 preservado em `card-469-idempotency-normalization-wip`.

Responsáveis: `#504` com `Responsável=Alan` (bug técnico de prova OOS). Sem card `Clara`. Nenhuma atualização de copy de produto/Drive além desta doc e do `kaizen-log` (Git é fonte).

## OpenSpec

- Pacote arquivado em 2026-08-17. Sync de specs: #469 (2 changes), #502, #503, #516-kaizen-509, #518. Skip-specs justificado: #504 (delta MODIFIED em spec ainda inexistente), #516-release-worktree-hygiene (requirement já existente), #517 (spec alvo inexistente), #550 e `design-planner-grok-4-6` (contrato OpenCode invertido pelo #562).
- Spec `cursor-harness` promovida e `developer-tooling` alinhada ao Cursor no closeout do #562.
- `openspec validate --all` pré-merge do código: **148/148**.

## Git e publicação

- `develop` continha o pacote homologado (HEAD `f4b89208`); história do #550/#559 permanece e o #562 remove `.opencode/` no HEAD.
- PR `develop -> main` #564, merge manual `91f5620e`.
- `release-guard pre`: PASS. CI do PR #564 verde (e2e da push em `develop` rerodado após timeout no protótipo walk-forward em DEV).

## Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `91f5620e` (`git fetch origin && git reset --hard origin/main`; merge PR #564).
- Migration: `alembic upgrade head` → `20260814_0001_discovery_sweeps` (head).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-yA8Lh7Oo.js` / `index-DRWrZ0va.css` servido em `/login` e `/app`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites`, `/discovery`, `/combo` → 200.

## Conclusão

- Release 2026-08-17 publicada em `main` (merge manual PR #564) com deploy PROD validado no commit `91f5620e`.
- Cards do pacote seguem `Homologado` até `release-guard post` PASS; só então avançam para `Pronto`.
