# Release 2026-08-17 — Lotes 1 e 2

Doc canônica do dia 2026-08-17. Dois pacotes Homologados publicados no mesmo dia; um arquivo só.

## Lote 1 — cards 469, 502, 503, 504, 516, 517, 518, 562

Cards `Homologado` incluídos:

- `#469` (P1, Codigo) — Backtest: descoberta sistemática de estratégias swing com leaderboard. Resp: Codex.
- `#502` (P1, Produto) — Combo: Ver logs inicia vazio e faz autoscroll. Resp: Codex.
- `#503` (P1, Produto) — Reavaliar holdout OOS com Treino (IS) separado. Resp: Codex.
- `#504` (P1, Codigo) — Digest canônico da prova OOS no round-trip JSON. Resp: Alan.
- `#516` (P0, Operacao) — Kaizen: completar #509 (GraphQL fail-closed e higiene de closeout). Resp: Codex.
- `#517` (P1, Operacao) — Kaizen: release-guard flagra OpenSpec terminal sem id. Resp: Codex.
- `#518` (P1, Operacao) — Kaizen: fechamento de release como gate. Resp: Codex.
- `#562` (P0, Codigo) — Migrar harness operacional para Cursor Agent (cutover OpenCode). Resp: Codex.

Fora do lote 1: `#550` e `#559` Cancelados (substituídos pelo #562). `#549` isolado em `card-549-unify-strategy-title-description` (Em Refinamento). WIP pós-homologação do #469 preservado em `card-469-idempotency-normalization-wip`.

Responsáveis: `#504` com `Responsável=Alan` (bug técnico de prova OOS). Sem card `Clara`. Nenhuma atualização de copy de produto/Drive além desta doc e do `kaizen-log` (Git é fonte).

OpenSpec lote 1: pacote arquivado em 2026-08-17. Sync de specs: #469 (2 changes), #502, #503, #516-kaizen-509, #518. Skip-specs justificado: #504 (delta MODIFIED em spec ainda inexistente), #516-release-worktree-hygiene (requirement já existente), #517 (spec alvo inexistente), #550 e `design-planner-grok-4-6` (contrato OpenCode invertido pelo #562). Spec `cursor-harness` promovida no closeout do #562. `openspec validate --all` pré-merge do código: 148/148.

Git lote 1: `develop` HEAD `f4b89208`. PR `develop -> main` #564, merge manual `91f5620e`. `release-guard pre`: PASS.

Deploy PROD lote 1: source `/srv/apps/prod/criptofarol/source` no `91f5620e`; `alembic upgrade head` → `20260814_0001_discovery_sweeps`; frontend `VITE_API_URL="/api" VITE_APP_ENV=production` bundle `index-yA8Lh7Oo.js` / `index-DRWrZ0va.css`; services `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`; URL `https://criptofarol.com.br` 200 e `/api/health` 200.

## Lote 2 — cards 529, 530, 531, 553, 554, 566, 567, 568

Cards `Homologado` incluídos:

- `#553` (P0, Codigo) — Migrar runtime do Cripto Farol de OpenClaw para Hermes. Resp: Codex.
- `#554` (P0, Operacao) — Corrigir skill de ambientes para DEV/PROD e Hermes. Resp: Codex.
- `#566` (P0, Codigo) — Worker de Discovery (dispatcher + Celery) em criptofarol.com.br. Resp: Codex.
- `#567` (P1, Codigo) — Chave de idempotência canônica no start/retry do sweep. Resp: Codex.
- `#529` (P0, QA) — Rota nova exige spec Playwright funcional+visual no qa-gate. Resp: Codex.
- `#530` (P1, Operacao) — Protótipo aprovado como spec de UI no apply. Resp: Codex.
- `#531` (P1, Operacao) — Verify e Code Review exigem evidência de task de UI. Resp: Codex.
- `#568` (P0, Codigo) — E2E de protótipo usa preview local, não a URL viva de DEV. Resp: Codex.

Fora do lote 2: `#569` em Aprovação de Design (`origin/card-569-code-review-bugbot` `059f6f38`). `#549` em Em Refinamento. WIP do #469 em `card-469-idempotency-normalization-wip`. Gerador automático de templates também em Em Refinamento.

Responsáveis: todos Codex. Sem card `Clara` ou `Alan`. Nenhuma atualização de copy de produto/Drive (Git é fonte).

### OpenSpec lote 2

- Changes arquivadas em 2026-08-17: `card-568-prototype-e2e-local`, `card-554-ambientes-skill-hermes`, `card-553-openclaw-to-hermes`, `card-566-prod-discovery-worker`, `card-529-new-route-playwright`, `card-530-prototype-ui-spec`, `card-531-ui-task-evidence`, `card-567-discovery-idempotency`.
- Specs novas: `hermes-agent-runtime`, `oracle-environment-map`, `prod-discovery-workers`, `new-route-playwright-coverage`, `prototype-as-ui-spec`, `ui-task-evidence`, `prototype-e2e-local-preview`.
- Sync em specs existentes: `ci-testing-hardening`, `delivery-qa-stage`, `design-approval-evidence`, `developer-tooling`, `card-close-evidence-integrity`, `cursor-harness`, `crypto-runtime-architecture`, `monitor-telegram-alerts`, `maintenance`, `discovery-sweep` (payload hash; idempotency key já estava no main spec).
- Skip-specs: `02-agent-chat-favorites.md` (já descrevia Hermes no spec legado).
- `openspec validate --all` no PR documental: 142/142. Pré-merge do código (changes ainda ativas): 143/143.

### Git e publicação lote 2

- `develop` continha só o pacote homologado (HEAD `e2a2af0c`).
- PR `develop -> main` #578, merge manual `2261ad56`.
- `release-guard pre`: PASS (worktrees extras removidas; WIP do #569 preservado no remoto; evidência do lote 1 usada só para o check da doc canônica já existente).
- CI do PR #578: checks iniciados verdes; `qa-gate` skip no PR para `main` (`base_ref != develop`).

### Deploy PROD lote 2

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `2261ad56` (`git fetch origin && git reset --hard origin/main`; merge PR #578).
- Migration: `alembic upgrade head` → `20260814_0001_discovery_sweeps` (head; nenhuma migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-DbfcRxXg.js` / `index-ClGHgnrB.css` servido em `/login`.
- Installer: `install-discovery-workers-systemd.sh --root /srv/apps/prod/criptofarol/source` → units PROD active.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Evidência operacional Discovery: dispatcher `RUN_DISCOVERY_OUTBOX_DISPATCHER=1` e loop started; Celery `discovery-prod` ready na fila `discovery`; favorite refresh permanece ligado.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites`, `/discovery`, `/combo` → 200.

## Conclusão

- Lote 1 publicado em `main` (PR #564, SHA `91f5620e`) e movido para `Pronto` no closeout da manhã.
- Lote 2 publicado em `main` (PR #578, SHA `2261ad56`) com deploy PROD validado; cards 529/530/531/553/554/566/567/568 seguem `Homologado` até `release-guard post` PASS; só então avançam para `Pronto`.
