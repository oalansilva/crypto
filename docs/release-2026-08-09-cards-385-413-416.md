# Release 2026-08-09 — Cards 385, 413, 416

## Pacote

Cards `Homologado` incluídos:

- `#385` — Monitor: comprar e vender ativo Spot direto pela Binance (venda a market). Resp: Codex. P1.
- `#413` — Carteira mais fácil leitura (zebra na tabela e remoção de textos redundantes). Resp: Codex.
- `#416` — Substituir gpt-5.6-luna por Qwen3.7 Plus no fluxo de análise visual (vision-router). Resp: opencode. P2.

Responsáveis: nenhum card com `Responsável=Clara` ou `Responsável=Alan` → passo documental de produto/Drive N/A para este pacote (sem docs de produto alterados).

## OpenSpec

- Changes ativas no pacote (ainda não arquivadas; archive acontece no fechamento):
  - `card-385-monitor-direct-spot-trading`
  - `wallet-easier-reading`
  - `card-416-substituir-luna-por-qwen3`
- `openspec validate --all`: **150/150 válidos** (evidência pré-merge).

## Git e publicação

- `develop` contém apenas conteúdo homologado do pacote + commits docs → PR direto `develop -> main`.
- Commits do pacote (já em `origin/develop`):
  - `95e7f7d4` — feat: concluir card #385
  - `288c17ad` — docs: fechar tasks técnicas do card #385
  - `985d1697` — feat: concluir card #413
  - `88a2a59d` — docs: fechar tasks técnicas do card #413
  - `c0045264` — feat: substituir gpt-5.6-luna por qwen3.7-plus no roteamento visual (card #416)
  - `59526270` — docs: classificar guard post da release 2026-08-09 (#435)
- PR `develop -> main` e merge manual (a preencher após merge).

## Classificação do release-guard pre (blockers)

Todos os 13 blockers são WIP local já integrado em `origin/develop` por squash (a checagem de ancestry do guard não reconhece squash) ou worktrees de evidência preservadas — classificados:

| Item | Classificação |
| --- | --- |
| branch local `card-416-substituir-qwen3` | já integrado (squash `c0045264`); limpar no pós-release |
| branch local `change-385-monitor-direct-spot-trading` | já integrado (squash `95e7f7d4`); limpar no pós-release |
| branch local `change-385-close` | já integrado (squash `288c17ad`); limpar no pós-release |
| branch local `change-413-wallet-easier-reading` | já integrado (squash `985d1697`); limpar no pós-release |
| branch local `change-413-close` | já integrado (squash `88a2a59d`); limpar no pós-release |
| branch local `docs-release-post-class` | já integrado (squash `59526270`); limpar no pós-release |
| worktree `crypto-worktrees/card-416-substituir-qwen3` | preservar até limpeza pós-merge |
| worktree `/tmp/criptofarol-card-385` | preservar até limpeza pós-merge |
| worktree `/tmp/criptofarol-card-385-close` | preservar até limpeza pós-merge |
| worktree `/tmp/criptofarol-card-413` | preservar até limpeza pós-merge |
| worktree `/tmp/criptofarol-card-413-close` | preservar até limpeza pós-merge |
| worktree `/tmp/criptofarol-card-413-preserve` (dirty) | preservar (evidência duplicada já em `origin/develop`); remover no pós-release |
| warning local main ≠ origin/main | normal; decisões usam origin/main |

## Deploy PROD

(a preencher após merge em main)

## Guard post e limpeza

(a preencher após publicar)
