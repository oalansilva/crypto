# Release 2026-07-31 — Stop Spot no Monitor, gate de Design no Kanban e Carteira stables

Doc canônica do dia 2026-07-31 (consolidada a partir de `release-2026-07-31.md` e `release-2026-07-31d.md` — higiene de docs duplicadas por data exigida pelo `release-guard post` do card #438).

## Release A — Cards 337, 340 (Stop Spot no Monitor e gate de Design no Kanban)

### Objetivo

Publicar em produção o lote homologado com proteção Spot stop-limit no gráfico do Monitor e o fluxo canônico de Design / aprovação por arraste no Kanban.

### Cards incluídos

- `#337` proteger posição Spot com stop-limit no gráfico do Monitor (inclui detecção de stop externo).
- `#340` adicionar fluxo de Design e aprovação por arraste no Kanban.

### Entregáveis

- Place/cancel de `STOP_LOSS_LIMIT` Spot no Monitor (qty 100% free, `cfstop_`), com remoção também de stop externo Spot.
- UI Proteção Spot no chart + copy de credenciais Binance com Spot trading opcional.
- Status canônicos `Design` / `Aprovação de Design` / `Pronto para Dev` / `Em desenvolvimento` e gate de design antes do DEV.
- Aprovação humana auditável `Aprovação de Design -> Pronto para Dev` (Alan).
- Adaptadores OpenSpec Codex/Cursor e skill `design-critic` alinhados.

### OpenSpec

- Changes arquivadas: `monitor-spot-stop-limit` e `card-340-design-approval-kanban-flow`.
- Specs sincronizados: `monitor-spot-stop-limit` (novo), `external-balances`, `user-preferences-binance-credentials`, `kanban`, `workflow-state-db`.

## Release B — Card 346 (Carteira: stables Spot + Simple Earn)

### Objetivo

Publicar em produção a correção homologada da Carteira para exibir saldos USDT/USDC (e demais stables) alinhados à Vista de ativos da Binance, incluindo Simple Earn Flexible.

### Cards incluídos

- `#346` Bug: USDC e USDT não aparecem na Carteira (Homologado).

### Entregáveis

- Fallback de preço ~1 USD para stables (Spot e wrappers `LD*`) sem omitir row por ticker ausente.
- UI da Carteira propaga `min_usd` até a API de balances.
- Snapshot prefere `Simple Earn` flexible/locked sobre `LD*` incompleto no `/api/v3/account`; fallback para `LD*` se Earn falhar.
- Total da Carteira alinhado à Vista de ativos (~Δ residual só de preço/timing).

### OpenSpec

- Change arquivada: `card-346-wallet-stable-balances-display`.
- Spec sincronizado: `external-balances`.

## Evidência esperada (ambas)

- `openspec validate --all`
- `scripts/release-guard pre`
- PR `develop -> main`, CI verde e merge manual
- atualização e restart dos serviços afetados em PROD
- validação pública de `https://criptofarol.com.br`
- `scripts/release-guard post`
- cards incluídos movidos para `Pronto`
