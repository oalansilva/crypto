# Release 2026-09-12

Doc canônica do dia 2026-09-12.

## Lote — cards 689, 886, 893, 895, 896, 906

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#689` (Seguranca, P1, Clara, Acesso) — convite de uso único e role de administrador persistida; auto-registro no e-mail admin sem verificação deixa de passar. `UI impact: none` (admin/API). OpenSpec `card-689-convite-uso-unico`. PR #903 `1c0ae1fb` (cherry-pick `e2e5a403`; inventário unitário 71 nesta branch, sem o #897 ainda Done). Residual P2 (corrida na 1ª emissão) permanece no card.
- `#886` (Operacao, P0, Clara, Monitor) — scan Telegram de alerta por timer systemd, sem Hermes; units DEV/PROD + wrapper fail-closed `BLOCK_PROD_BOT`. `UI impact: none`. OpenSpec `card-886-telegram-systemd-timer`. PR #892 `ce6c26d3` (cherry-pick `7d94b52b`).
- `#893` (Operacao, P1, Alan) — teto de review em silêncio: classificar, um auto-fix, residual no Done sem Ask. `UI impact: none`. OpenSpec `card-893-teto-review-silencio`. PR #894 `284a61b2` (cherry-pick `b6357c97`). Archive `--skip-specs` (headers ADDED já no main spec).
- `#895` (Operacao, P1, Alan) — review emite schema; destape é tabela; fecho não relitiga residual. `UI impact: none`. OpenSpec `card-895-review-schema-destape`. PR #898 `24209b38` (cherry-pick `b43acbf3`).
- `#896` (Produto, P1, Clara, Metricas) — Calmar de calendário no ranking da Descoberta e selo NO-GO visível. `UI impact: affected`. OpenSpec `card-896-discovery-calmar-nogo`. PR #900 `113cbf7c` (cherry-pick `65da2a31`).
- `#906` (Produto, P1, Clara, Metricas) — Sharpe, Win% e CAGR nas grelhas Acompanhar/Decidir, sem expandir a linha. `UI impact: affected`. OpenSpec `card-906-discovery-metrics-grid`. PR #907 `03697afd` (cherry-pick `157954fb`).

Fora do lote (não publicados): `#897` `#899` `#904` em **Done** na `develop` (não homologados). Caminho `release-*` a partir de `main` + cherry-picks, não `develop → main`. Worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` / `#825` e as do pacote/Done preservadas via `PRESERVED_BRANCHES`. Branch remota órfã `origin/card-469-idempotency-normalization-wip` (WIP fora do lote). Kaizen nascidos neste closeout: `#909` `#910` `#911`.

Responsáveis Clara: `#689` `#886` `#896` `#906`. Responsáveis Alan: `#893` `#895`. Copy de landing **inalterada**. Pin overlay `v1.1.15`.

### OpenSpec

- Changes arquivadas: `card-886-telegram-systemd-timer` → `openspec/changes/archive/2026-09-12-card-886-telegram-systemd-timer/` (spec `monitor-telegram-alerts`); `card-893-teto-review-silencio` → `…/2026-09-12-card-893-teto-review-silencio/` (`--skip-specs`); `card-895-review-schema-destape` → `…/2026-09-12-card-895-review-schema-destape/`; `card-896-discovery-calmar-nogo` → `…/2026-09-12-card-896-discovery-calmar-nogo/`; `card-689-convite-uso-unico` → `…/2026-09-12-card-689-convite-uso-unico/` (spec nova `single-use-invite-access`); `card-906-discovery-metrics-grid` → `…/2026-09-12-card-906-discovery-metrics-grid/`.
- Main specs atualizadas: `monitor-telegram-alerts`, `cursor-code-review`, `cursor-harness`, `llm-flow-emission`, `discovery-leaderboard`, `discovery-three-modes`, `admin-user-management`, `beta-lead-access-hardening`, `closed-beta-access-control`, `single-use-invite-access` (nova).
- `openspec validate --all`: 166/166 na `release-2026-09-12` após archive.
- Ativas nesta branch de release: nenhuma. Na `develop` permanecem as changes dos cards Done (`#897` `#899` `#904`).

### Git e publicação

- `origin/develop` `03697afd` continha o pacote **e** três Done não homologados (`#897` `#899` `#904`). Pacote isolado em `release-2026-09-12` (base `origin/main` `daef3a6b` + cherry-picks).
- `release-guard pre` PASS (`RELEASE_CARDS=689,886,893,895,896,906`; `PRESERVED_BRANCHES` in-flight + leftover + `wip-board-prototype`; `RELEASE_DATE=2026-09-12`). Comentários Homologado canónicos dos seis cards postados neste turno antes do `pre`. Campos `#906` Clara/P1/Produto/Metricas e `#689` Responsável=Clara preenchidos neste turno.
- PR `release-2026-09-12 → main` #908, merge `216a72f8b5233aef1845e49ad3d72cb75a5d3078` (`Merge pull request #908 from oalansilva/release-2026-09-12`).
- CI do PR #908: checks `pass` (`backend-tests` 2m6s, `backend-unit-tests` 2m45s, `e2e-playwright` 4m54s, `openspec-validate` 18s, `process-fsm` 48s, `frontend-build` 44s, `frontend-lint`, `frontend-tests`, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`, `backend-format`, `changes`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 908 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-12 → main` (este commit) com `docs/release-2026-09-12.md` + `docs/kaizen-log.md` + `docs/mvp-scope.md` + `docs/decision-log.md`.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final). `#897` `#899` `#904` voltam à `develop` **depois** do `post`.

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (root-owned; operações com `sudo`).
- Commit publicado: `216a72f8b5233aef1845e49ad3d72cb75a5d3078` (merge PR #908).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl; `20260909_0001 → 20260915_0001` role admin + convites `#689`).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CMHlCJ7i.js` / `index-BvYYCoMc.css` (1,790.35 kB / 212.15 kB, gzip 542.92 kB / 33.52 kB). Hash JS novo vs lote 2026-09-10 (`index--3lXkM0I.js`) por Descoberta #896/#906.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service`. Timer Telegram: `install-telegram-alert-scan-systemd.sh --env prod` (timer já enabled desde 2026-09-10; oneshot exit 0). Primeiro curl público `/api/health` 502 na janela de boot (~4s); retry 200. Recidiva #888.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200; `/monitor` serve `index-CMHlCJ7i.js`.

### Evidência de deploy

`216a72f8b5233aef1845e49ad3d72cb75a5d3078 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-12 (subsecção lote 689/886/893/895/896/906), com os cards ainda Homologado. Filho isolado `fecho-lote kaizen`. Cards novos `#909` `#910` `#911`. Dedupe `#888` `#759` `#658` `#904`.

### Drive

Sincronização Google Drive da Clara: ver evidência no closeout (gog). `#825` está Cancelado; falha de credencial não reabre card.
