# Release 2026-09-05

Doc canônica do dia 2026-09-05.

## Lote — cards 803, 817, 818, 819 e 820

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#803` (Produto, P0, Clara, Monitor) — HOLD sem alvo derivado (não é take-profit). Card e modal: `distância até saída` → `distância até stop` → `stop` → `entrada` → `preço atual`. `UI impact: affected`. OpenSpec `card-803-monitor-remover-alvo`. PR #829 `4a8f7d4e`.
- `#817` (Operacao, P0, Alan, kaizen) — dsh não envia `reasoning.effort=none` (spawn vazio apply/review). `UI impact: none`. OpenSpec `card-817-dsh-reasoning-effort`. PR #836 `518f2ca1`. Residual 8.1 (dump GUI dsh autenticado) permanece `[ ]`.
- `#818` (Operacao, P0, Alan, kaizen) — dsh grill spawn distingue citação de papel. `UI impact: none`. OpenSpec `card-818-dsh-grill-spawn-cite`. PR #834 `e693a631`.
- `#819` (Operacao, P0, Alan, kaizen) — protótipo de superfície existente MUST clonar a página viva no `index` (landing + catálogo). `UI impact: none` (harness). OpenSpec `card-819-clone-pagina-viva`. PR #835 `de1e08f2`.
- `#820` (Operacao, P0, Alan, kaizen) — cota GraphQL pelos cabeçalhos; REST na superfície issue. `UI impact: none`. OpenSpec `card-820-graphql-quota-rest`. PR #830 `0100198f`.

Fora do lote: worktrees in-flight `#472` / `#600` / `#604` / `#606` / `#614` / `#720` / `#728` / `#837` (preservadas via `PRESERVED_BRANCHES`). `#840` kaizen nascido neste closeout em Em Refinamento.

Responsáveis Clara: `#803`. Responsáveis Alan: `#817` `#818` `#819` `#820`. Copy de landing **inalterada**. Pin overlay `v1.1.8`. `docs/mvp-scope.md` item 5 alinhado ao HOLD sem alvo (este PR documental).

### OpenSpec

- Changes arquivadas: `card-803-monitor-remover-alvo` → `openspec/changes/archive/2026-09-05-card-803-monitor-remover-alvo/`; `card-817-dsh-reasoning-effort` → `openspec/changes/archive/2026-09-05-card-817-dsh-reasoning-effort/` (`openspec archive -y` com 1 task `[ ]` 8.1, specs synced); `card-818-dsh-grill-spawn-cite` → `openspec/changes/archive/2026-09-05-card-818-dsh-grill-spawn-cite/`; `card-819-clone-pagina-viva` → `openspec/changes/archive/2026-09-05-card-819-clone-pagina-viva/`; `card-820-graphql-quota-rest` → `openspec/changes/archive/2026-09-05-card-820-graphql-quota-rest/`.
- Main specs: `opportunity-monitor` (já no merge #838), `covenant-flow`, `cursor-code-review`, `process-harness`, `design-route-clone-gate`, `impeccable-design-gate`, `llm-flow-emission`, `process-fsm-event`, `documental-board-evidence-validation`, `grill-card`, `kaizen-continuous-improvement`, `process-fsm-guard`, `process-fsm-paging`, `release-worktree-hygiene`.
- `openspec validate --all`: 167/167 no `pre`; 162/162 após archive (5 changes saíram).
- Ativas restantes: nenhuma.

### Git e publicação

- `origin/develop` `de1e08f2` continha só o pacote homologado (`#829` `#830` `#834` `#835` `#836`).
- `release-guard pre` PASS (`RELEASE_CARDS=803,817,818,819,820`; `PRESERVED_BRANCHES` 13 in-flight/leftover; `RELEASE_DATE=2026-09-05`). Homologation comments canônicos dos cinco cards postados neste turno antes do `pre`. Campos Responsável/Tipo/Frente preenchidos neste turno.
- PR `develop -> main` #838, merge `f32785a6930610a76ceb391c4a106b914624af24` (`Merge pull request #838 from oalansilva/develop`).
- CI do PR #838: checks `pass` (`backend-tests` 2m5s, `e2e-playwright` 4m59s, `openspec-validate` 21s, `process-fsm` 30s, `frontend-build` 38s, `qa-policy`, `new-route-playwright-coverage`, `backend-coverage-gate`) + `qa-gate` / `deploy-staging` `skipping` (base `main` esperado). Watcher `timeout 35m gh pr checks 838 --watch --fail-fast --interval 20` verde. `mergeable=MERGEABLE` `mergeStateStatus=CLEAN`.
- Docs PR: `docs-release-2026-09-05 -> main` (este commit) com `docs/release-2026-09-05.md` + `docs/kaizen-log.md` + `docs/mvp-scope.md` + `docs/decision-log.md` + 5 archives + specs.
- Sync `main → develop`: obrigatório após docs merge (árvores idênticas antes do `post` final).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `f32785a6930610a76ceb391c4a106b914624af24` (merge PR #838).
- Migration: `alembic upgrade head` em `backend/` (PostgresqlImpl, já head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-DNi9d2DD.js` / `index-BSHopo8Y.css` (1,778.32 kB / 212.66 kB, gzip 539.78 kB / 33.58 kB). Hash JS novo vs lote 2026-09-03 (`index-jZpK-fWS.js`) pelo HOLD sem alvo #803. CSS inalterado.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-leads.service`, `criptofarol-prod-runtime-worker.service` (`criptofarol-prod-candle-writer.service` é timer, inactive/waiting). Primeiro `GET /api/health` durante o boot devolveu 502; retry → 200.
- Endpoints validados: `https://criptofarol.com.br/api/health` → 200 `{"status":"ok","service":"crypto-backtester-api"}`; `https://criptofarol.com.br/` → 200 (copy Spot inalterada); `/help` e `/monitor` 200 servindo `index-DNi9d2DD.js`.

### Evidência de deploy

`f32785a6930610a76ceb391c4a106b914624af24 services=criptofarol-prod-backend,criptofarol-prod-frontend,criptofarol-prod-leads,criptofarol-prod-runtime-worker url=https://criptofarol.com.br`

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-09-05, com os cards ainda Homologado. Card novo `#840` (mvp-scope/decision-log defasados no card de produto). Dedupe `#658` `#769` `#759` `#825`.

### Drive

Sincronização Google Drive da Clara **não executada**: `gog` recusou (`OAuth client credentials missing`, sem `~/.config/gogcli/credentials.json`); `bws secret list` recusou (`Missing access token`). Markdown local `docs/mvp-scope.md` versionado neste PR; Drive fica pendente de credencial nesta sessão. Coberto por `#825`.
