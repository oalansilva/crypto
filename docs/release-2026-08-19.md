# Release 2026-08-19 — Lote cards 549, 582, 599, 609

Doc canônica do dia 2026-08-19.

## Lote — cards 549, 582, 599, 609

Cards `Homologado` incluídos (Alan em chat: `tenho vários cards homologados suba a release`):

- `#549` (P1, Codigo) — Unificar título e descrição da estratégia em Favoritos, Monitor e Descoberta (edição global no Combo). Resp: Codex.
- `#582` (P2, Comercial) — Palestra Agile Brazil 2026: Do upstream ao deploy — automatizar o downstream sem remover gates humanos. Resp: Alan.
- `#599` (P1, Codigo) — Discovery: métricas de ranking N/A e RSI scalping zerado em 1d. Resp: Clara.
- `#609` (P0, Operacao) — Harness: `process-fsm.yaml` compilável (T0–T17 + fixtures). Resp: Clara.

Fora do lote: `#605` em QA (`card-605-discovery-walk-forward-split`). `#610` e `#614` em Aprovação de Design. `#611` em Todo. `#608`/`#612`/`#613` em Em Refinamento. `#606` Cancelado (worktree preservada).

Responsáveis Clara/Alan: `#599` e `#609` (Clara), `#582` (Alan). Documentação de produto/processo atualizada neste closeout (`decision-log`, `kaizen-log`, deck já versionado em `docs/palestra-upstream-deploy.md`). Copy de landing inalterada.

### OpenSpec

- Changes arquivadas em 2026-08-19: `unify-strategy-title-description`, `card-582-palestra-upstream-deploy`, `fix-discovery-leaderboard-metrics`, `card-609-process-fsm-yaml`.
- Specs novas: `process-fsm`, `palestra-upstream-deploy`.
- Sync em specs existentes: `strategy-template-descriptions` (hierarquia título+descrição + editor Combo), `discovery-leaderboard` (métricas no path sem walk-forward).
- CLI archive com `--skip-specs` após sync agent-driven.
- `openspec validate --all`: 145/145.

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `ff85eb71`) mais o código dos 4 cards. Push do archive em `develop` recusado (proteção `qa-gate`).
- Branch `release-2026-08-19` = `ff85eb71` + archive `0ec5dd77`.
- `release-guard pre`: PASS (`RELEASE_CARDS=549,582,599,609`; `PRESERVED_BRANCHES` para in-flight #605/#610/#614/#472/#606 e leftovers).
- Comentários Homologado canônicos nos 4 cards antes do `pre` (#609 já existia; 549/582/599 postados neste turno).
- PR `release-2026-08-19 -> main` #616, merge `c6762a27`.
- CI do PR #616: checks iniciados verdes; `qa-gate` skip no PR para `main` (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `c6762a27` (`git fetch origin && git reset --hard origin/main`; merge PR #616).
- Migration: `alembic upgrade head` → `20260818_0001` (`display_name` em `combo_templates`).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-A5R04R0w.js` / `index-Cyx126Ee.css` servido em `/login`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`, `criptofarol-prod-discovery-worker.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`; `/login`, `/app`, `/dashboard`, `/monitor`, `/favorites`, `/discovery`, `/combo` → 200.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-19, com cards ainda Homologado. Cards kaizen #617 (P1) e #618 (P2) criados em Em Refinamento; F-2 (Homologado sem comentário) é recidiva do #579.
