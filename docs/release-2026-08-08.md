# Release 2026-08-08 — Cards 361, 384, 395 e 399

## Pacote

- `#361` — Revisar nomes e descrições das estratégias em Favoritos e Monitor (visão trader).
- `#384` — Reorganizar análise gráfica de Favoritos e eliminar informações duplicadas.
- `#395` — Migração Codex -> opencode: config, skills, MCPs, hooks.
- `#399` — Roteamento automático de análise de imagens (GPT-5.6 Luna).

Todos os cards estavam `Homologado` no board antes da publicação.

## OpenSpec

- Changes arquivadas em `openspec/changes/archive/2026-08-08-*`:
  - `card-361-strategy-transparency`
  - `card-384-favorites-analysis-hierarchy`
  - `card-395-migracao-opencode`
  - `card-399-vision-router`
- Delta specs sincronizadas para as specs principais:
  - `strategy-transparency` (modificada: `Safe functional transparency`; adicionadas: `coordinated visual ownership`, `trader-facing language`)
  - `favorites` (adicionadas: `decision-first reading hierarchy`, `progressive disclosure`, `canonical signals`, `responsive layouts`)
  - `developer-tooling` (nova)
  - `visual-analysis-routing` (nova)
- `openspec validate --all`: **146/146 válidos** após archive.

## Git e publicação

- PR `develop -> main`: #405, merge manual em `952cd387` (27 commits: 4 cards + archive).
- PR de sync `main -> develop`: #406, merge em `879fe86a`.
- Guard `pre`: PASS após classificação (worktrees/branches de cards integrados limpas; WIP do card 385 preservado).
- Guard `post`: conteúdo alinhado (árvores `origin/develop` e `origin/main` idênticas); blocker topológico classificado (delta é apenas o merge commit do sync, sem conteúdo exclusivo pendente).

## Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source` (worktree limpa, branch `main`, sem stash).
- Commit publicado: `d237b078` (`git fetch origin && git reset --hard origin/main`).
- Migration: `alembic upgrade head` aplicou `20260803_0001_set_short_template_direction` (persist `direction: short` no template `short_ema200_pullback`); `alembic current` = `20260803_0001 (head)`.
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV="production" npm run build` gerou `dist/assets/index-CsUwUyjV.js` / `index-DWWkcXQ9.css`.
- Services reiniciados e ativos: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`, `criptofarol-prod-runtime-worker.service`; `criptofarol-prod-leads.service` intacto.
- Endpoints validados: `http://127.0.0.1:8003/api/health` e `https://criptofarol.com.br/api/health` → `{"status":"ok"}`; `https://criptofarol.com.br/combo/results` servindo o bundle novo (HTTP 200); `/` continua servindo a landing (esperado).
- Banco PROD `crypto_app`: `direction=short` confirmado no template `short_ema200_pullback`.

## Preservação de WIP

- WIP do card `#385` (Monitor: comprar/vender ativo Spot direto pela Binance) — outra change em `Code Review`, fora do pacote — preservado.
- Refs locais de preservação criadas antes de limpar branches intermediárias:
  - `refs/preserve/card-384-remove-signal-noise-20260808`
  - `refs/preserve/card-384-remove-signal-noise-evidence-20260808`
- Branch `sync-main-into-develop-20260808` e `release-20260808-cards-361-384-395-399` usadas como veículos de PR.

## Validação

- CI do PR #405 (base `main`): todos os checks verdes (backend-format/tests/unit-tests, coverage-gate, frontend-build/lint/tests, e2e-playwright, changes, openspec-validate, qa-policy).
- `qa-gate` é condicionado a PRs com base `develop` (workflow `ci.yml`); evidência do gate verde veio no PR #404 (archive/sync) e no PR #406 (sync).
- Cards movidos para `Pronto` após o merge em `main` **e** o deploy PROD validado acima.

## Correção de processo

- A release foi inicialmente fechada movendo os cards para `Pronto` sem deploy PROD (a regra local definia `Pronto = main` e a skill `alan-workflow-ambientes` não havia sido carregada). O deploy PROD acima corrigiu o runtime; `AGENTS.md`, `rules.md` e `docs/decision-log.md` foram atualizados para exigir deploy PROD (source no commit publicado + migrations + build + restart + URL pública validada) antes de `Pronto`, e para carregar `alan-workflow-ambientes` em qualquer pedido de release/publicação/lote.
