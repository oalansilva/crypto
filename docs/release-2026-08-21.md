# Release 2026-08-21

Doc canônica do dia 2026-08-21 (três pacotes).

## Lote — card 652

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#652` — T16 live: Agent move Homologado→Pronto após `release-guard post` PASS

Fora do lote: in-flight `#472`/`#604`/`#606`/`#614`.

### OpenSpec

- Change arquivada: `card-652-t16-live-fechar-release` → `openspec/changes/archive/2026-08-21-card-652-t16-live-fechar-release/`.
- Sync: `process-fsm`, `process-fsm-event`, `cursor-harness`.
- `openspec validate --all`: verde no CI do PR #654.

### Git e publicação

- `origin/develop` continha só o pacote (`49590820`).
- Branch `release-2026-08-21` = `49590820` + archive `a59cf185`.
- `release-guard pre`: PASS (`RELEASE_CARDS=652`; `PRESERVED_BRANCHES` in-flight + leftover #652).
- Comentário Homologado canônico postado neste turno.
- PR `release-2026-08-21 -> main` #654, merge `0cb8aaf4`.
- Sync `main → develop` PR #655.

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `0cb8aaf4`.
- Migration: `alembic upgrade head` (já head; sem migration nova neste lote harness).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CbEIETTN.js` / `index-q-WncaRa.css`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints: `https://criptofarol.com.br/` → 200; `/api/health` → 200; interno 8003/5173 → 200.

### Kaizen

Entrada em `docs/kaizen-log.md` (este lote) com o card ainda Homologado. Closeout Pronto via `process_event fechar_release` (T16 live).

## Lote — cards 617 / 618 / 625 / 631 / 632 / 637

Cards `Homologado` incluídos (Alan em chat: `suba a release`):

- `#617` — closeout quando `develop` exige `qa-gate`: archive OpenSpec via `release-*`
- `#618` — `pre` em `release-*` não bloqueia `develop` local não publicado
- `#625` — Guard: redirect nulo (`/dev/null`, `/tmp`) não é write de produto
- `#631` — Guard: citar sidecar de digest em git add/commit/status não é deny
- `#632` — T14 live fecha Done quando `qa-gate` está verde
- `#637` — Monitor: pagar compra Spot com USDC mantendo estratégia BASE/USDT

Fora do lote: `#604` Aprovação de Design; `#608` Em Refinamento; in-flight `#472`/`#606`/`#614`.

### OpenSpec

- Changes arquivadas em 2026-08-21: `card-617-*` … `card-637-*` → `openspec/changes/archive/2026-08-21-card-*/`.
- Sync de specs: `release-archive-via-release-branch` (nova), `release-worktree-hygiene`, `process-fsm-guard`.
- `#632` / `#637`: archive com `--skip-specs` (headers `MODIFIED` divergentes do main spec); justificativa operacional.
- `openspec validate --all`: verde no CI do PR #648.

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `05691619` antes do archive).
- Branch `release-2026-08-21` = `05691619` + archive `6a71b015` (reuso do nome após o lote 613).
- `release-guard pre`: PASS (`RELEASE_CARDS=617,618,625,631,632,637`; `PRESERVED_BRANCHES` in-flight + leftovers do pacote).
- Comentários Homologado canônicos postados neste turno (antes do `pre`).
- PR `release-2026-08-21 -> main` #648, merge `33df201a`.
- Sync `main → develop` PR #649, merge `0bee4033` (árvores idênticas).
- CI do PR #648: checks iniciados verdes; `qa-gate` e `deploy-staging` skip (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `33df201a` (`git fetch origin && git reset --hard origin/main`; merge PR #648).
- Migration: `alembic upgrade head` → `20260818_0001 -> 20260821_0001` (lock Spot orders por símbolo da estratégia).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-CbEIETTN.js` / `index-q-WncaRa.css`.
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`. Interno `127.0.0.1:8003/api/health` e `:5173/` → 200.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-21 (este lote), com os cards ainda Homologado. Sem card kaizen novo; fricções cobertas pelos próprios #617/#618/#625/#631/#632.

## Lote — card 613

Cards `Homologado` incluídos (Alan em chat: `suba a release`, após o ensaio e-to-e do epic #608):

- `#613` (P2, Operacao) — Harness: paging de contexto (`sessionStart` + `harness.mdc` curto + stub `AGENTS.md`). Resp: Clara.

Fora do lote: `#608` Em Refinamento (epic). `#614` Aprovação de Design (artigo). In-flight `#472`/`#606`. `#609`–`#612` já Pronto em lotes anteriores.

Responsáveis Clara/Alan: `#613` (Clara). Documentação atualizada neste closeout (`decision-log`, `kaizen-log`, `project-hub`). Copy de landing inalterada.

### OpenSpec

- Change arquivada em 2026-08-21: `card-613-process-fsm-paging` → `openspec/changes/archive/2026-08-21-card-613-process-fsm-paging/`.
- Spec nova: `process-fsm-paging`.
- Sync em spec existente: `cursor-harness` (stub `AGENTS.md`, budget `harness.mdc`, prioridade δ e Guard, página Moore no `sessionStart`).
- Archive por `shutil.move` + sync agent-driven (CLI `--skip-specs` não usada; equivalente operacional).
- `openspec validate --all`: 149/149 (CI PR #638).

### Git e publicação

- `origin/develop` continha só o pacote homologado (HEAD `771784ab`, PR #636).
- Branch `release-2026-08-21` = `771784ab` + archive `6e58e4e4`.
- `release-guard pre`: PASS (`RELEASE_CARDS=613`; `PRESERVED_BRANCHES` para in-flight #472/#606/#614/#584 e leftover #613).
- Comentário Homologado canônico no #613 postado neste turno (antes do `pre`).
- PR `release-2026-08-21 -> main` #638, merge `7df18d54`.
- CI do PR #638: checks iniciados verdes; `qa-gate` e `deploy-staging` skip no PR para `main` (`base_ref != develop`).

### Deploy PROD

- Source: `/srv/apps/prod/criptofarol/source`.
- Commit publicado: `7df18d54` (`git fetch origin && git reset --hard origin/main`; merge PR #638).
- Migration: `alembic upgrade head` (já era head; sem migration nova neste lote).
- Frontend: `VITE_API_URL="/api" VITE_APP_ENV=production npm run build` → bundle `index-A5R04R0w.js` / `index-Cyx126Ee.css` (inalterado; lote harness).
- Services PROD reiniciados: `criptofarol-prod-backend.service`, `criptofarol-prod-frontend.service`.
- Endpoints validados: `https://criptofarol.com.br/` → 200; `/api/health` → 200 `{"status":"ok"}`. Health público 502 nos primeiros segundos após o restart; 200 após warmup (~8s). Interno `127.0.0.1:8003/api/health` e `:5173/` → 200.

### Kaizen

Entrada canônica em `docs/kaizen-log.md` na data 2026-08-21 (lote 613), com o card ainda Homologado. Sem card kaizen novo; recidivas cobertas por #579, #617, #625/#631.
