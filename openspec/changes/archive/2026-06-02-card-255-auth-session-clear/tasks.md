## 1. Auth State Sync

- [x] 1.1 Add an invalid-session notification from `authFetch` when refresh fails after `401`.
- [x] 1.2 Make `AuthProvider` clear stored user state and React Query cache when that notification is received.

## 2. Validation

- [x] 2.1 Add focused frontend coverage for the invalid-session notification path.
- [x] 2.2 Run OpenSpec validation and focused frontend build/test checks.

## Project Skills

- Use local project/frontend/debugging skills when the implementation expands beyond this focused auth-state fix.
