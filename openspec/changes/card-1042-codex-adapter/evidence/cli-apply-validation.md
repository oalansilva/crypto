# Evidência de validação CLI — Apply

Data: 2026-09-25 UTC. Host: `instance-20260525-0958`. Codex CLI `0.156.1`, OpenSpec `1.5.0`, Python `3.12.3`. Shell sem `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, `NO_PROXY` e `OPENAI_BASE_URL`. Ensaios de hook ocorreram no clone descartável confiado `/tmp/card1042-cli-apply-bcgQ1KrH/repo`, com fixture `gh` local que devolvia Status explícitos e registrava consultas; nenhum Status do board real foi alterado. Sem Codex Cloud ou bypass de trust/sandbox.

## Skills e OpenSpec em sessão nova

`python3 scripts/process-fsm/codex_skills.py check --source <clone> --target <clone>` retornou `Codex skill bridge coverage/drift: PASS`. Sessão CLI nova descobriu as skills `covenant-flow` e `openspec-verify-change`, recebeu Status `Em desenvolvimento` e orientação para ler a skill canônica quando aplicável. Sessão: `/tmp/card1042-cli-apply-bcgQ1KrH/skills-meta-session.jsonl`, SHA-256 `0a13f4e045500f5711d5fd549bb11bf665a837b0d34ea03c14c4776fb65c79b4`.

Em outra sessão nova, `/usr/bin/openspec instructions apply --change card-1042-codex-adapter --json` executou via Bash do Codex, exit 0; a resposta reportou 12 tarefas completas e 8 pendentes e avisou que a regra `tasks` não era uma lista de strings e foi ignorada. Sessão SHA-256 `6c45a23bdfce10273cbed72c7e3500f604c2d211c20c74f6ced7df84531cecb7`. Uma tentativa anterior falhou em `bwrap` antes de executar OpenSpec; após correção do perfil oficial AppArmor, esta execução e as provas abaixo rodaram em sandbox normal (`read-only`/`workspace-write`).

## Deny/allow com o hook real

O wrapper de fixture `gh` forneceu Status `Todo`, `Design`, `Pronto para Dev` e `Em desenvolvimento` nos cenários correspondentes. O clone usou a definição original de `.codex/hooks.json` (SHA-256 `69df18690f540e11cf990d803541b6bb26d13bc67dba202ffc1e580394f9b83d`). Para validar o guard candidato, copiei apenas o `guard.py` do produto para o clone: ambos têm SHA-256 `aceccaee9ae9cca7431ac8bdaf0c02eaeba9500c00b9874a1bf610580f85e809`; `codex_adapter.py` também coincidiu nos dois lados (`68c98e7f4f3a45edc598fe69d78bee35e798f461c2b29dfac7c1aa6385d1062d`).

- `Bash` em `develop` e `Todo` foi negado. Os arquivos descartáveis ficaram byte a byte intactos: `frontend/src/.codex-hook-probe.txt` SHA-256 `59be9051e36eb1e79d011bada8221fdf68af6f8f997f2bcd9d9dee5440f3d126`; `frontend/src/.codex-todo-command.txt` SHA-256 `d6d3666b8d10448aa52799c1d9f018ebe3c1fb01d81b1c11281a02dcfc1104e0`.
- `apply_patch` em `develop`, `Todo` e `Pronto para Dev` foi negado; sentinelas permaneceram inalteradas, respectivamente SHA-256 `9c51251920c917c17d0944f65044b9aba2b9155a9830b09bcc97c652a466cee3`, `59db18c2ba793866733f5323d2f9a076fb239f5f6bb612540adfed26922103da` e `ef175c9bbe2c5e19454f253d3f8fb313fb9a112b1d704f84ed6f9301e609cb53`.
- Em `Design`, uma chamada `apply_patch` criou o artefato descartável `.codex-design-allow-probe.md`; PreToolUse não retornou deny. Sessão SHA-256 `23a427a484939eb33e0ac451917e61eb8bca88767faa17efab28deae076c0ff4`.
- Após T8, uma chamada válida `apply_patch` (`*** Add File`) criou `frontend/src/.codex-after-t8-valid-patch.txt`; o CLI fez readback `AFTER_T8_VALID`. SHA-256 do arquivo `dd124624c20977a9fe7cc70173ece00a5f9590e8e5edb158dfe96541584d999f`; sessão SHA-256 `3d174507f6bbca78f45af08fdabd0d57f3753735eb9f40452334eb9e36f383b4`. A tentativa imediatamente anterior foi permitida pelo hook, mas o harness recusou patch malformado; não a conto como prova de escrita. A repetição válida confirmou escrita e readback.

A inspeção minimizada do envelope real de `apply_patch` registrou apenas formato: `tool_input` era objeto com a chave `command` do tipo string; o comando continha as instruções do patch. O guard foi atualizado para extrair os caminhos desse formato, e um golden sanitizado foi adicionado. Trace de forma SHA-256 `59f0e314f76f852ebd07afa89b9b1e5c58e27011413048397a0d0379b558b3de`; testes focados de adapter/hooks: `23 passed`. O matcher declara `Bash|exec_command|apply_patch|Edit|Write|Agent`; esta rodada exerceu Bash e `apply_patch`. Spawn nativo não entrou em `PreToolUse`, limite cooperativo aceito por Alan no comentário do #1042 e detalhado em `cli-spawn-routing-p0.md`. Não foi feito ensaio IDE local: Alan a retirou do recorte #1042.
