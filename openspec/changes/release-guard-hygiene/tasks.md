## Tasks

- [x] Add `scripts/release-guard` with `audit`, `pre`, and `post` modes.
- [x] Update `AGENTS.md` to make the release guard mandatory before and after release closure.
- [x] Stop tracking local/generated files already covered by `.gitignore`.
- [x] Run focused validation for the guard and OpenSpec change.
