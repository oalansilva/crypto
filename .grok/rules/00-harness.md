# Process harness (Grok)

Always-on δ lives in root `AGENTS.md`. Do not copy columns, I1–I9, or the 12-column runbook here.

If `.grok/rules/process-fsm-page.md` exists, MUST Read it and treat it as the Moore page for `(q, bound_card, q_git)`.
If it is missing, resolve the tuple via `scripts/process-fsm` before product writes. A missing page is not an allow token.
